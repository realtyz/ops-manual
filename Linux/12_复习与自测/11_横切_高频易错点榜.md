---
tags:
  - Linux
  - 复习方法
  - 易错点
  - 自测
created: 2026-09-19
---

# 横切：高频易错点榜

> [!cite] 参考资料
> 本套笔记各阶段子笔记里的「常见坑」表与 00 号主笔记的「故障现场定位表」；阶段 3、4、5、6、7、8、9 各章的考点速览。凡涉及默认值与版本差异处，均以对应阶段的适用版本说明为准（本文以 RHEL 8/9 系与 Debian/Ubuntu LTS 系为主线）。
>
> **实测状态**：榜单中的数字**转引自前十一章在本实验机的实测记录**（转引条件以对应阶段为准）；带「文档值」字样的是 man 页与官方文档结论，未在本机复现。榜单本身不引入新的机制结论。本实验机为 **Ubuntu 24.04.5 LTS（VMware 虚拟机）/ 内核 `6.8.0-139-generic` / systemd 255（`255.4-1ubuntu8.17`）/ 4 vCPU / `MemTotal` 7894 MiB / root 可用**。

> **这篇讲什么**：八十四组「常听到的说法 vs 事实」。它既是易错点清单，也是**最省时间的自测题**——先看左列，自己说出右边，再对答案。
>
> **必须先读什么**：[[Linux/12_复习与自测/02_前置_证据与口径|02 前置：证据与口径]]（判断事实时先看口径）。
>
> **读完能交付**：一份标出「我踩过哪几条」的个人易错清单，并进入错题本。
>
> **所属**：横切篇；练习技能 **R3 证据与口径、R6 限时答辩**

## 0. 30 秒速览

> [!abstract] 这一篇只要记住五句话
> - **这张榜的用法是「遮住右列自测」，不是通读。**
>   - 怎么验证：逐行只看左边，说出右边；说不出的那几行直接进错题本。
> - **绝大多数「事实」都长成同一句话：你看到的量，不是你以为的那个量。**
>   - 证据：`free` 的 `used` 不等于「被应用占用」；`%util` 不等于设备饱和度；`load` 不等于 CPU 使用率。
> - **跨章误答集中在「同名不同物」：**
>   - 证据：`cache`、`queue`、`limit`、`state` 四个词在本套笔记里各有多套语义。
> - **看到「一定能」「只要…就」要立刻警惕。**
>   - 怎么验证：把它改写成「在什么条件下成立」，条件写不出来的，通常就是错的。
> - **每条错法都要落到「第一反应不要做什么」。**
>   - 理由：知道错在哪只是第一步；能从第一反应里改掉它，才叫会。

## 1. 内存与资源

| 常见做法或说法 | 事实 | 出处阶段 |
| --- | --- | --- |
| `free` 显示可用内存很少，说明内存不够 | 判据是 `MemAvailable` + PSI；缓存占用是正常工作状态。**转引**阶段 10 实测：触碰 3 GiB 后 `MemAvailable` 7224 → 4145 MiB，而 PSI 的 `avg10/60/300` 全程为 0（累计 `total` 在涨：some 298→9695、full 208→9477） | 阶段 4（数字转引阶段 10） |
| 退出码 137 就是被 OOM Killer 杀了 | 137 只说明被 `SIGKILL`（`128+9`）；人工 `kill -9` 同样是 137。OOM 的定性证据是内核日志与 cgroup 的 `memory.events` | 阶段 3、4 |
| `VSZ` 很大就是内存泄漏 | 申请 ≠ 占用；`malloc` 只是虚拟地址承诺，首次写入才通过缺页拿到物理页 | 阶段 4 |
| `malloc` 成功说明内存已经到手 | 真正的落地发生在首次写入；`overcommit` 允许承诺总量超过物理内存 | 阶段 4 |
| `vm.swappiness=0` 等于关闭 swap | 它是回收倾向的权重，不是开关；压力下仍可能换页。真要禁用只能 `swapoff` 或 `memory.swap.max=0` | 阶段 4 |
| 关掉 swap 更安全 | 只是把「换页变慢」换成「被 OOM 杀掉」 | 阶段 4 |
| 「内存去哪了」只有一种答案 | 三类主要答案：tmpfs/共享内存（`Shmem`）、内核 slab 与 dentry/inode 缓存（`Slab`）、被 cgroup 计入的页缓存 | 阶段 4 |
| 容器被 OOMKilled 一定是宿主机内存不够 | 多数是 cgroup 级 OOM，整机可能还有空闲内存；要同时看宿主侧与容器侧 | 阶段 3、4、11 |
| 容器里 `top` / `free` 反映容器的真实约束 | 看到的是整机口径；容器受限情况要看 cgroup 文件（`memory.max`、`cpu.max` 等） | 阶段 10、11 |
| 改 `/etc/security/limits.conf` 能改服务限制 | systemd 服务读 unit 的 `LimitXXX=` / `TasksMax=`；`limits.conf` 只影响经 PAM 的登录会话 | 阶段 3、8 |
| `ulimit -n` 改了当前服务的上限 | 它只影响当前 shell 及其子进程；运行中的服务不受影响 | 阶段 3 |

## 2. 进程、信号与调度

| 常见做法或说法 | 事实 | 出处阶段 |
| --- | --- | --- |
| `kill -9` 是常规手段 | 它是最后手段；`SIGTERM` 可捕获、可优雅退出，是 `systemctl stop` 的第一发 | 阶段 3 |
| `kill -9` 能杀掉一切进程 | `D` 状态进程的信号被挂起，等它回到用户态才处理；僵尸进程已经终止，等的是**父进程**回收 | 阶段 3 |
| 僵尸进程占 CPU / 内存 | 它已退出，只占进程表项；要处理的是父进程未回收的问题 | 阶段 3 |
| `ps` 数出来的进程数就是线程数口径 | 内核调度的基本单位是 task；`ps -eLf` 数的是 task，两者不是一回事 | 阶段 3 |
| 主机上的 PID 1 能被 `kill -9` 杀掉 | 内核不执行这些信号的默认终止动作；普通用户拿到 `EPERM`。容器里的 PID 1 受同样保护，但可从宿主按宿主 PID 杀掉 | 阶段 1、3 |
| 负载高就是 CPU 忙 | `load` 把 `D` 状态（不可中断睡眠）也算进去，且必须除以核数 | 阶段 10 |
| 整机 CPU 不高就排除 CPU 问题 | 单核饱和：4 vCPU 上单核跑满时整机 `%usr` 就是 25%（1/4），`load` 只有 0.34（**转引**阶段 10 实测） | 阶段 10 |
| `vmstat` 的 `si` 是软中断 | `si`/`so` 是换入/换出；软中断看 `mpstat` 的 `%soft` | 阶段 10 |
| 上下文切换多就说明系统有问题 | 要分自愿与非自愿：自愿切换多在等 IO / 等锁，非自愿切换多才是 CPU 竞争（**转引**阶段 10 实测：socket ping-pong 自愿 40,343.67 次/秒、非自愿 16.67 次/秒） | 阶段 3、10 |

## 3. 存储与 IO

| 常见做法或说法 | 事实 | 出处阶段 |
| --- | --- | --- |
| `df` 和 `du` 对不上，说明 `du` 不准 | 四类原因：已删除但被进程持有（`lsof +L1`）、被挂载覆盖的目录、保留块与元数据、稀疏文件与延迟分配 | 阶段 5 |
| `rm` 后 `df` 没降，说明文件系统有问题 | 文件可能仍被进程持有；**转引**阶段 9 实测：在 64 MiB 的临时 loop 文件系统里删掉一个仍被持有的 **10 MiB** 文件后，`df` 已用从 24576 字节涨到 10510336 字节，而 `du -sh` 只有 **20K**，`/proc/<pid>/fd/4` 显示 `(deleted)`，关掉 fd 后回落到 24576 字节 | 阶段 5、9 |
| 「磁盘满」就是容量满 | 还有 inode 满、ext4 保留块（默认 5%）、配额超限（报 `Disk quota exceeded`）三种 | 阶段 5 |
| XFS 可以缩容 | XFS 只能扩不能缩；ext4 能缩但必须卸载且风险高 | 阶段 5 |
| 可以在挂载状态下跑 `e2fsck` | 必须在卸载（最稳）或只读挂载下做；XFS 用 `xfs_repair`，且默认拒绝对已挂载设备操作 | 阶段 5 |
| 文件系统变只读，先 `mount -o remount,rw` | 只读是内核的保护动作；先查原因（`dmesg` 的 ATA/SCSI/NVMe 错误、阵列状态） | 阶段 5 |
| `%util` 100% 说明盘已经饱和 | 在 NVMe 上 `%util` 常常接近 100% 却与延迟无关；要连起来读 `await`、`aqu-sz` | 阶段 5、10 |
| `await` 高就是盘慢 | `await` 含排队时间，且与请求规模强相关；**转引**阶段 10 实测：`w/s=324 × w_await=6.59 ms = 2.14`，与同窗口 `aqu-sz=2.14` 吻合（Little's law）；而另一个窗口 `%util=98.80%` 时 `aqu-sz` 才 3.15、`w_await` 只有 0.09 ms、`wareq-sz` 只有 4 KiB | 阶段 10 |
| IO 慢就是盘坏 | 要先看是否有回写积压、备份抢 IO、日志同步落盘；健康计数（`smartctl` 介质错误）通常早于阵列降级出现 | 阶段 5、10 |
| RAID 5 能抵御任意两块盘同时故障 | RAID 5 只容忍单盘故障；重建期间再坏一块就会丢数据，而大容量盘的重建时间很长——重建窗口本身就是风险窗口 | 阶段 5 |
| LVM 扩容只要 `lvextend` 就完成了 | `lvextend` 只改逻辑卷，还要扩文件系统（`lvextend -r`，或 `xfs_growfs` / `resize2fs`）；验收标准是 `df`，不是 `lvs` | 阶段 5 |
| 配了 RAID 就等于有了备份 | RAID 只防单块盘故障，不防误删、勒索加密、逻辑损坏与整机故障；备份的验收标准仍然是「能恢复并计时」 | 阶段 5、11 |

## 4. 网络

| 常见做法或说法 | 事实 | 出处阶段 |
| --- | --- | --- |
| `tcp_fin_timeout` 能改 `TIME_WAIT` 时长 | 它管的是 `FIN_WAIT2` 的超时；`TIME_WAIT` 由内核常量 `TCP_TIMEWAIT_LEN` 固定 **60 秒**（**转引**阶段 6 实测：0.5 秒采样下 **62.06 秒仍在、63.07 秒消失**，多出的 2~3 秒是 `close()` 到进入 TIME-WAIT 的间隔加采样粒度），没有任何 sysctl 能改 | 阶段 6 |
| `CLOSE_WAIT` 多要调内核参数 | 它是应用收到 FIN 却没调 `close()`；只能改应用 | 阶段 6 |
| `TIME_WAIT` 是异常状态 | 它是主动关闭方的正常状态；过多时先看是否短连接为主，再考虑连接复用 | 阶段 6 |
| 调大应用 backlog 就能解决队列溢出 | 上限是 `min(backlog, somaxconn)`；实测 `listen(10000)` 在 `somaxconn=4096` 的机器上被静默截断成 4096 | 阶段 6 |
| 半连接队列满一定丢包 | 现代发行版默认 `tcp_syncookies=1`，内核改发 cookie，计数落在 `TcpExtSyncookiesSent`，`ListenDrops` 不增长 | 阶段 6 |
| `ping` 通就说明网络没问题 | 只证明 ICMP 可达；TCP 可能因防火墙、队列溢出或应用未监听而失败 | 阶段 6 |
| 客户端偶发超时、服务端无日志就是「玄学」 | 常见现场是全连接队列溢出后内核静默丢 SYN；证据在 `nstat` 的 `TcpExtListenOverflows`/`ListenDrops` | 阶段 6 |
| `ss -lnt` 的 `Recv-Q` 是队列上限 | `Recv-Q` 是当前队列深度，`Send-Q` 才是上限；`Recv-Q > Send-Q` 说明队列已满 | 阶段 6 |
| 抓包是排查网络的第一步 | 先看计数器（`nstat`、`ss -ti`），再决定要不要抓包；全量抓包会影响性能且抓不完 | 阶段 6、10 |

## 5. systemd 与服务

| 常见做法或说法 | 事实 | 出处阶段 |
| --- | --- | --- |
| 改了 unit 文件就生效 | 完整链条是「改文件 → `status` 出现 changed on disk 警告、manager 里仍是旧值 → `daemon-reload` 后 manager 变新值、运行中进程环境仍是旧值 → `restart` 后才是新值」 | 阶段 8 |
| `daemon-reload` 等于重启服务 | 它只重读 unit 定义，不重启进程 | 阶段 8 |
| `Wants=` 就等于 `After=` | 前者决定「要不要拉起对方」，后者只决定顺序；不写 `After` 就并行启动 | 阶段 8 |
| `Type=simple` 万能 | 进程自己后台化时会被控制组清掉（实测立刻 `inactive (dead)`）；后台化的服务要用 `Type=forking` 或改成 `Type=simple` + `ExecStart` 前台运行 | 阶段 8 |
| `Type=notify` 一定能等到通知 | 默认 `NotifyAccess=main`；子进程发通知会被拒，一直等到超时（**转引**阶段 8 实测：改成 `NotifyAccess=all` 后 0.0396 秒启动成功） | 阶段 8 |
| `tmpfiles` 的 age 只看 mtime | 默认同时看 mtime/atime/ctime 且取最「严」的口径；「mtime 40 天前但 ctime 是今天」不会被 `--clean` 删除，要用 `m:30d` 明确 | 阶段 8 |
| cron 的环境和你登录的 shell 一样 | 实测只有 `HOME/LOGNAME/PATH/LANG/SHELL=/bin/sh/PWD`；`PATH` 与别名都不同，`%` 必须写成 `\%` | 阶段 8 |
| 沙箱选项报错是业务问题 | 常见 `status=226/NAMESPACE`、`216/GROUP`、`203/EXEC` 都是 unit 配置导致的 | 阶段 8 |

## 6. 权限与安全

| 常见做法或说法 | 事实 | 出处阶段 |
| --- | --- | --- |
| 权限拒绝只需要 `chmod` | `Permission denied` 可能来自五层：DAC 位、ACL、capabilities、挂载选项、SELinux/AppArmor；用 `namei -l`、`getfacl`、`getcap`、`ls -Z` 分层看 | 阶段 7 |
| 目录的 `r` 就能删里面的文件 | 删文件看**目录的 `w`**；目录的 `x` 才是「能穿过」，`r` 是「能列出名字」 | 阶段 7 |
| `/tmp` 的 `1777` 只是为了方便 | sticky 位的作用是只允许文件属主（或 root）删除自己的文件 | 阶段 7 |
| 给脚本加 SUID 就能提权 | 内核忽略脚本上的 SUID；SUID 只对二进制生效 | 阶段 7 |
| 用 `find -perm 4000` 找 SUID 文件 | `-perm 4000` 是精确匹配；要找「包含 SUID 位」的文件必须用 `-perm -4000` | 阶段 7 |
| 账号默认设置已经够安全 | 默认 `PASS_MAX_DAYS=99999`、`INACTIVE=-1`（不过期），加固必须主动收紧 | 阶段 7 |
| `setenforce 0` 是修复手段 | 它只是把强制拦截关成 permissive（审计与 AVC 拒绝记录**仍在产生**）；正确做法是看 AVC 拒绝记录并调整策略或上下文 | 阶段 7 |
| 看配置文件就知道 SSH 的最终策略 | `Match` 会改变最终值；用 `sshd -T -C user=...,addr=...` 看生效结果 | 阶段 7 |
| 加固的验收是「感觉关掉了」 | 验收标准是**可核查**：账号、SUID/SGID/capabilities、监听端口、计划任务与自启、SSH 授权五类基线 | 阶段 7 |
| 关掉 SSH 密码登录就算加固完成 | 还要禁 root 直登、只留必要认证方式、限制可登录用户/组、限制尝试次数与并发，并用 `sshd -T` 看 `Match` 之后的最终生效值 | 阶段 7 |

## 7. 启动、内核与时间

| 常见做法或说法 | 事实 | 出处阶段 |
| --- | --- | --- |
| 内核版本号可以直接跨发行版比大小 | RHEL 会把新特性回移进较老的主线版本（RHEL 9 基线 5.14、Ubuntu 24.04 是 6.8、Debian 12 是 6.1） | 阶段 1 |
| 改了 `/etc/default/grub` 就生效 | 必须重新生成配置（`update-grub` 或 `grub2-mkconfig`），验收标准是 `cat /proc/cmdline` | 阶段 2 |
| 启动参数和 `sysctl` 是一回事 | `/proc/cmdline` 里的启动参数只在启动那一刻生效；`sysctl` 改的是运行时参数 | 阶段 2 |
| 系统起不来就重装 | 五环各有现场（固件屏 / GRUB 提示符 / initramfs shell / emergency mode）；先判断「现在是谁在输出」 | 阶段 2 |
| 机器莫名重启过，重启一次再看看 | 重启会覆盖现场；先取 `journalctl -b -1`、`/var/crash/`、带外日志 | 阶段 2 |
| 「重启能解决 90% 的问题」是好经验 | 它同时毁掉 100% 的现场；生产上应先取证再决定是否重启 | 阶段 1 |
| 时钟漂移只影响日志好不好看 | 跨机日志比对、告警时序、证书校验、令牌与审计时间线全都依赖时钟 | 阶段 9、11 |
| 手动 `date -s` 对时最快 | 会造成时间跳变、破坏增量日志与监控数据；应当修 NTP/chrony | 阶段 9 |

## 8. 可观测性与生产

| 常见做法或说法 | 事实 | 出处阶段 |
| --- | --- | --- |
| 日志写满就先删大文件 | 先看谁在写、文件是否仍被进程持有；**转引**阶段 9 实测：`rm` 一个仍被持有的 **10 MiB** 文件后 `df` 已用反而从 24K 涨到 10 MiB | 阶段 9 |
| `copytruncate` 是最安全的轮转方式 | **inode 不变、原地截断**：持有旧 fd 的进程继续写**同一个 inode**（不会写进归档），代价是截断与写入之间有丢数据窗口。「持有旧 fd 的进程把后续内容写进归档文件」其实是 **`create` 模式**的行为（改名后旧 fd 仍写旧 inode，需在 `postrotate` 里让应用重开文件）——本机实测：`copytruncate` 后 `app.log` inode 不变，`create` 后 `app.log` 换新 inode、旧 fd 的写入落进 `app.log.1` | 阶段 9 |
| 平均值正常就说明服务正常 | 平均值掩盖长尾：1000 个样本（950×20 ms + 45×100 ms + 5×2000 ms）平均 33.5 ms，而 `max` 2000 ms | 阶段 9、10 |
| p95 只有一个标准算法 | 同一组样本按四种常见口径分别是 20.0 / 24.0 / 96.0 / 100.0 ms——报分位数必须连算法一起报 | 阶段 9 |
| `top` 首屏是自开机以来的平均 | **说反了**：本机实测首屏是**很短的一段差分**——机器已开机 4 小时，自旋进程启动 3 秒后跑 `top -b -n1`，首屏即报 `%Cpu(s): 26.2 us`（若真是开机均值应接近 0）。首屏不能单独下结论的真实理由是：默认**整机视角**、默认**不展开线程**、窗口只有一两秒 | 阶段 10 |
| `strace` 是排查慢问题的首选 | **转引**阶段 10 实测约 **45 倍**减速（基线 51 ms → 2280 ms）；应短时、定向、单进程使用，或先用 `/proc` 与计数器 | 阶段 10 |
| 备份任务成功就说明备份可用 | 备份的唯一验收标准是「能恢复」并计时（实测 RTO）；快照与副本不算备份 | 阶段 11 |
| 配了 VIP 就等于高可用 | 判据是「拔掉一台之后业务还剩多少」，取决于单实例、流量切换、连接排空与余量 | 阶段 11 |
| 3-2-1 原则就够了 | 还要加「1 份离线或不可变」，防止在线备份被勒索软件一并加密 | 阶段 11 |
| 参数改了不生效就再改一次 | 先查加载顺序：目录合并按文件名排序、同名文件 `/etc` 优先、最后应用 `/etc/sysctl.conf`，**后加载覆盖先加载** | 阶段 11 |
| 「改动很小」可以不做变更评审 | 影响面与可逆性才是分级依据；「小改动」造成的故障占比很高 | 阶段 11 |
| 证书浏览器能打开就说明配置正确 | 浏览器会自动补链；要用 `openssl s_client -servername` 从外部验证并看 `notAfter` | 阶段 11 |
| 复盘写到「以后小心」就够了 | 合格的改进项要写成「动作 + owner + 期限 + 验收方式」 | 阶段 11 |
| USE 方法就是把监控工具挨个跑一遍 | USE 是按资源过三问：利用率（Utilization）、饱和度（Saturation）、错误（Errors）；先问「哪个资源的哪一项」，再决定用哪个工具 | 阶段 10 |
| trace 是日志的替代品 | 三者互补：日志回答「发生了什么」，指标回答「整体趋势」，trace/span 回答「这一次请求的时间花在哪一段」 | 阶段 9 |
| `sar` 随时都能查到历史数据 | 前提是装了 sysstat 且历史采集已启用。**本实验机是正面样本**：`sysstat-collect.timer` `enabled`、`/var/log/sysstat/sa19` 存在（23096 字节）；但很多机器上 `/var/log/sysstat` 根本不存在——**先确认有数据，再谈复盘** | 阶段 9 |
| 火焰图什么都能看 | 它通常只反映采样到的 on-CPU 栈；off-CPU 等待、缺少符号或权限受限时采不到，而且横轴是样本占比、不是时间轴 | 阶段 10 |

## 9. 生产动作：把榜单变成自己的清单

> [!example]- 生产动作：遮住右列做一次自测（生产机也可以做，回顾为主）
> **怎么做**：把本页八张表的右两列遮住，逐行只看左列，说出事实与出处阶段。说不出的行标一个圆点，统计每组的圆点数。
> **预期**：圆点分布会告诉你薄弱领域——如果是「存储与 IO」组集中出错，下一轮补缺就定在阶段 5。
> **风险**：无。**耗时**：约 40 分钟。**回滚**：不需要。
> **验收**：每组圆点 ≤ 2 个；超过的组，把对应行抄进错题本并排复查日期。

> [!example]- 生产动作：挑五条在本机只读复验（生产机也可以做）
> **怎么做**：从榜单里挑五条能只读复验的（例如 `free` 与 PSI、`df`/`du` 关系、`ss -lnt` 的 `Recv-Q`/`Send-Q`、`sysctl --system` 顺序、`tmpfiles --clean` 的 age 语义），逐条在本机跑一遍并把输出贴进错题本。
> **预期**：至少有一条的本机表现与你以为的不同——那条就是你真正的盲区。
> **风险**：只读，无风险；涉及 `tmpfiles --clean` 时用自建目录，不要指向系统目录。**耗时**：约 40 分钟。**回滚**：删除自建目录。

## 常见坑（关于「用这份榜」本身）

| 常见做法或说法 | 后果或事实 |
| --- | --- |
| 把这份榜通读一遍就以为会了 | 通读只带来熟悉感；必须遮住答案做提取 |
| 只记右边的「事实」，不记左边的「说法」 | 考场上你要识别的是**错误说法**，记住左列才有用 |
| 不标明出处阶段 | 补缺时无法定位到篇；榜单每行都给出来源 |
| 把带条件的结论当成绝对结论 | 例如「100 秒」与「60 秒」都可能是对的，取决于哪个状态、哪个版本 |
| 只自测一次 | 按 D+1 / D+3 / D+7 复查，圆点才会真正消失 |
| 只做纸面自测，不复跑命令 | 数字背后的口径与输出形态才是最容易丢的部分 |

## 决策练习

> 同事说：「这台机器的 `%util` 已经 99% 了，IO 到瓶颈了，赶紧换盘。」你先做什么？
>
> - **A. 同意换盘**，`%util` 99% 说明设备一直在忙。
> - **B. 先看 `await`、`aqu-sz` 与业务延迟是否同步上升，并确认设备类型**——`%util` 高常常不代表饱和；**转引**阶段 10 实测：`%util=98.80%` 时 `w_await` 只有 0.09 ms，而另一个窗口 `%util` 只有 80.10%、`w_await` 却有 6.59 ms。
> - **C. 直接看 `du` 找出最大的目录，删掉一些。**

**为什么选 B**：`%util` 是「有 IO 在飞行的时间比例」，不携带队列深度与延迟信息；必须与 `await`、`aqu-sz`、业务延迟一起读，并且看设备类型。

**为什么 A 不对**：换盘是重大变更，基于一个不完整的指标下结论风险极高。

**为什么 C 不对**：容量与 IO 饱和度是两个不同的问题；删文件既不解决 IO，还可能触发磁盘回收压力。

## 要点自测

> [!question]- 这张榜单属于哪一种复习工具？它为什么比「通读笔记」有效？
> - **判定**：它把「错误说法」当作线索，逼你先说出事实，属于主动回忆。
> - **影响什么**：识别错误说法的能力，正好对应面试与现场被追问时的判断。
> - **不影响什么**：它不替代原篇；每条的完整机制仍在对应阶段。
> - **落点**：遮住右列，逐行自测，标圆点，进错题本。
>
> [!question]- 榜单里的「事实」通常可以概括成哪一句话？
> - **判定**：你看到的量，往往不是你以为的那个量。
> - **例子**：`free` 的可用量 ≠ 内存压力；`%util` ≠ 设备饱和度；`load` ≠ CPU 使用率；`Recv-Q` ≠ 队列上限。
> - **影响什么**：决定你会不会用一个错口径得出一个错结论。
> - **第一反应不要是什么**：不要看到指标异常就先改配置或重启。
>
> [!question]- 为什么「第一反应不要做什么」比「正确答案」更重要？
> - **判定**：正确答案让你说得好，第一反应决定你做得好。
> - **影响什么**：真实故障中，一个错误的第一反应（重启、`remount rw`、全量抓包）能把小问题放大。
> - **不影响什么**：两者不冲突；先记住不要做什么，再把正确动作补上。
> - **落点**：每一条圆点错题都要写一句自己的「不要做」。

> 上一篇：[[Linux/12_复习与自测/10_横切_场景题答题骨架|10 横切：场景题答题骨架]] ｜ 下一篇：[[Linux/12_复习与自测/12_横切_端到端演练|12 横切：端到端演练]]
