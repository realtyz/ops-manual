---
tags:
  - Linux
  - 运维面试
  - 学习大纲
created: 2026-09-15
---

# Linux 运维学习大纲

> **定位**：一条面向运维/SRE 面试的学习路径——不是把命令再过一遍，而是把「生产中真正会遇到的问题」和「面试官真正会追问的点」对齐。
> **前置基础**：会日常 Shell 操作、懂文件权限的基本概念、用过 `top`/`ps`/`systemctl`、能读写简单的 bash 脚本。
>
> 原则：每个阶段按**里程碑**过关，章内按「原理 → 体系 → 实操 → 判例 → 治理」展开。每一条的标准不是「知道这个命令」，而是「能讲清它的语义、边界，以及出问题时怎么用它定位」。

---

## 学习路径总览（Roadmap）

| 阶段  | 主题              | 过关方式   | 产出目标                                    |
| --- | --------------- | ----- | --------------------------------------- |
| 1   | 实验环境与操作纪律       | 里程碑驱动 | 有可快照的实验机，能交出基线卡、变更记录与演练记录                |
| 2   | 启动流程、内核与系统初始化   | 里程碑驱动 | 讲清从加电到登录的全链路，能救回一台起不来的机器                |
| 3   | 进程、信号与资源隔离      | 里程碑驱动 | 能定位进程卡死、杀不掉、进程数打满；讲清 namespace/cgroup   |
| 4   | 内存管理与 OOM       | 里程碑驱动 | 讲清「够不够」与「谁在用」两本账，能定性 OOM、配内存上限并复盘 |
| 5   | 存储、文件系统与 IO     | 里程碑驱动 | 讲清 inode / 挂载 / LVM / RAID，处理磁盘满与 IO 瓶颈 |
| 6   | 网络协议栈与排障 ★      | 里程碑驱动 | 按六站分层定位连接/丢包/吞吐问题，用 `ss`/`tcpdump` 取证   |
| 7   | 权限、账号与安全加固      | 里程碑驱动 | 按十站定位权限拒绝与提权面，能按基线加固一台生产机并做应急取证         |
| 8   | systemd、服务与定时任务 | 里程碑驱动 | 能排查服务起不来，正确写 unit 与 timer               |
| 9   | 日志、监控与可观测性      | 里程碑驱动   | 建立日志轮转与指标采集，告警能落到具体动作                   |
| 10  | 性能分析与故障排查 ★     | 里程碑驱动 | 用一套方法论把 CPU / 内存 / IO / 网络问题定位到根因       |
| 11  | 生产运维、变更与高可用     | 里程碑驱动   | 独立完成一次带回滚的生产变更，交得出变更单、恢复记录与 Runbook    |
| 12  | 复习与自测           | 持续    | 交得出错题本、一页纸、限时答辩记录与端到端演练记录            |

> 标 ★ 的 **阶段 6（网络）** 与 **阶段 10（性能排障）** 是运维面试区分度最高的两块：命令谁都会敲，能从现象推到根因的人不多，优先打透。

> **不在本大纲范围内的部分**：GPU 驱动与内核模块、PCIe/NUMA 拓扑、RDMA/RoCE 无损网络、perftest 基线与 XID 硬件故障处理属于 AI 基础设施主题域，见 [[AI-Infra/00_简介|AI-Infra 大纲]]。本大纲只在阶段 6 与阶段 10 保留通用主机侧的调优与观测入口，重复的概念不在这里展开。
>
> 容器运行时（Docker/containerd、镜像与联合文件系统存储驱动）与编排层（Kubernetes 对象、控制器、CNI/CSI）属于容器主题域，见 [[Kubernetes/00_简介|Kubernetes 大纲]]。本大纲只保留容器赖以成立的三个宿主侧机制（namespace / cgroup / 联合文件系统），以及「站在宿主机上看容器」的排障入口。

---

## 这套大纲怎么用

**真正在考什么**

不是「你记得多少命令」，而是**从现象到根因的推理链**。任何一道运维题，都用同一个骨架作答：

> 现象与影响面 → 分层定位（哪一层）→ 用什么命令取证据 → 结论与根因 → 止损动作 → 长期改进

**三条练习纪律**

- 每个知识点都要能回答三问：**是什么、边界在哪、生产上怎么用**。
- 每个现象都要准备一份「**第一反应不要是什么**」。会讲「不要随手 `kill -9`、不要在生产机上不限流抓包、不要用 `setenforce 0` 掩盖问题」，比多背十个命令更能体现经验。
- 所有结论尽量带**量化**：负载除以核数、`si/so` 是否长期非 0、`await` 与业务延迟的对应关系。只说「感觉慢」在面试里等于没答。

**与 Kubernetes 笔记的关系**

本套笔记是 [[Kubernetes/00_简介|Kubernetes 大纲]] 的前置。阶段 3（cgroup/namespace）、阶段 6（网络）会直接复用到 [[Kubernetes/03_调度_资源_QoS]] 与 [[Kubernetes/04_网络]]，排障方法论也是同一套；两套笔记建议交叉复习。

---

## 阶段 1：实验环境与操作纪律

**入口**：[[Linux/01_实验环境与操作纪律/00_导读与知识地图|01 实验环境与操作纪律]]（原理 4 篇 · 体系 1 篇 · 实操 1 篇 · 判例 1 篇 · 治理 1 篇，共 9 篇）

**这条线研究什么**：在一台机器上安全工作需要哪些前提——这台机器是什么（发行版与版本基线）、程序为什么跑不起来（依赖解析）、弄坏了能不能回来（快照与状态一致性）、现场怎么留住（证据的易失性与观测的代价）。主线是**一次手工变更**从接手、变更前、执行、复验、取证到收口的过程；横切线是发行版差异、资料与工具来源、风险分级。

**过关里程碑**（不按天数）

1. **复述**：合上笔记画出「一次手工变更的六站」，说清四类现象（命令找不到 / 程序缺依赖 / 配置不生效 / 回滚后对不上）的现场在哪；
2. **只读基线**：在一台机器上完成一次只读基线采集并留下基线卡（生产机上也可以做）；
3. **可回滚变更**：完成一次带回滚命令与触发条件的变更，并用三层验收给出结论；
4. **演练与收口**：在能快照、能进控制台的实验机上做一次破坏性演练，交出演练记录（含恢复耗时）与一段 Runbook。

学习内容清单、生产实践与自测已并入章内各篇：清单与现场定位表见该章 `00_导读`，逐项内容与自测见各篇。

---

## 阶段 2：启动流程、内核与系统初始化

**入口**：[[Linux/02_启动流程与内核/00_导读与知识地图|02 启动流程与内核]]（原理 4 篇 · 体系 1 篇 · 实操 1 篇 · 判例 1 篇 · 治理 1 篇，共 9 篇）

**这条线研究什么**：控制权从固件到登录入口的五段交接——每一段凭什么交出控制权、交不出去时现场在哪、哪些变更会让它交不出去。要练出的判断是：面对「起不来」先问「现在是谁在输出」，并知道哪几类证据重启即失。

**过关里程碑**（不按天数）

1. **复述**：合上笔记画出「控制权的五段交接」，说清四类现象（开机没输出 / 停在提示符 / 挂在半路 / 起来了但服务没好）的现场在哪；
2. **只读基线**：在一台机器上完成一次启动基线采集并留下基线卡（生产机上也可以做）；
3. **可回滚变更**：完成一次带回滚命令与触发条件的启动类变更（内核参数持久化或重建 initramfs），并用重启后的读数验收；
4. **演练与收口**：在能快照、能进控制台的实验机上做一次恢复演练（写坏 `fstab` 或 `rd.break` 重置密码），交出演练记录（含恢复耗时）与一段 Runbook；有条件时补一次 panic 取证演练。

学习内容清单、生产实践与自测已并入章内各篇：清单与现场定位表见该章 `00_导读`，逐项内容与自测见各篇。

---

## 阶段 3：进程、信号与资源隔离

**入口**：[[Linux/03_进程与信号/00_导读与知识地图|03 进程与信号]]（原理 4 篇 · 体系 1 篇 · 实操 1 篇 · 判例 1 篇 · 治理 1 篇，共 9 篇）

**这条线研究什么**：一个进程从被创建到被回收的全过程——`fork` 继承了什么、`execve` 保留了什么、它在等谁、信号为什么可以延迟生效、退出之后谁来收尾。要练出的判断是：面对「卡死 / 悄悄消失 / 起不来 / 变慢」四类现象，先确认它在不在、属于哪一类，再按证据定性，而不是先动手。

**过关里程碑**（不按天数）

1. **复述**：合上笔记画出「一个进程的一生」四站，说清四类现象（卡死杀不掉 / 悄悄消失 / 起不来 / 变慢）的现场在哪，并指出哪些证据重启即失；
2. **只读基线**：在一台机器上完成一次只读基线采集并留下进程基线卡，含关键服务的主进程、实际生效的限制与所在 cgroup（生产机上也可以做）；
3. **可回滚变更**：给一个服务把任务数或句柄上限改到正确的一层，写出验证命令与回滚命令，并用进程实际生效的值验收；
4. **复盘**：完成一次故障注入（或一次真实处置），交出死亡复盘记录（含「谁发的信号」的定性依据）与一段可复用的 Runbook。

学习内容清单、生产实践与自测已并入章内各篇：清单与现场定位表见该章 `00_导读`，逐项内容与自测见各篇。

---

## 阶段 4：内存管理与 OOM

**入口**：[[Linux/04_内存管理与OOM/00_导读与知识地图|04 内存管理与 OOM]]（原理 4 篇 · 体系 1 篇 · 实操 1 篇 · 判例 1 篇 · 治理 1 篇，共 9 篇）

**这条线研究什么**：一块内存从被申请到被杀的全过程——申请只是记账、缺页才落到物理页、缓存与回写、回收与换页，最后到 OOM 的定性与复盘。要练出的判断是：面对「内存不够」与「内存去哪了」，先分清是哪一本账，再按证据定性。

**过关里程碑**（不按天数）

1. **复述**：合上笔记画出「一块内存的五站」，说清四类现象（内存去哪了 / 有内存却 OOM / 越用越慢 / 悄悄被杀）的现场在哪；
2. **只读基线**：在一台机器上完成一次只读基线采集并留下基线卡（生产机上也可以做）；
3. **可回滚变更**：给一个服务配好内存上限，写出验证命令与回滚命令；
4. **复盘**：完成一次故障注入（或一次真实处置），交出 OOM 复盘记录与一段可复用的 Runbook。

学习内容清单、生产实践与自测已并入章内各篇：清单与现场定位表见该章 `00_导读`，逐项内容与自测见各篇。

---

## 阶段 5：存储、文件系统与 IO

**入口**：[[Linux/05_存储与文件系统/00_导读与知识地图|05 存储与文件系统]]（原理 4 篇 · 体系 1 篇 · 实操 1 篇 · 判例 1 篇 · 治理 1 篇，共 9 篇）

**这条线研究什么**：一块盘从被认出来到退役的全过程——内核怎么认识它、文件系统在上面放下哪些结构、怎么挂进目录树、容量怎么长大、坏了怎么发现与换掉。要练出的判断是：面对「有空间却写不进去」「IO 变慢」「文件系统变只读」这三类现象，先分清是哪一本账、哪一层链路、哪一种结局，再按证据动手。

**过关里程碑**（不按天数）

1. **复述**：合上笔记画出「一块盘的服役一生」五站，说清三类现象（写不进去 / IO 变慢 / 写坏变只读）的现场在哪；
2. **只读基线**：在一台机器上完成一次只读基线采集并留下基线卡（生产机上也可以做）；
3. **可回滚变更**：完成一次挂载变更与一次在线扩容，各自写出验证命令与回滚方式；
4. **复盘**：完成一次故障注入（或一次真实处置），交出一份含证据、根因、动作与验收的记录。

学习内容清单、生产实践与自测已并入章内各篇：清单与现场定位表见该章 `00_导读`，逐项内容与自测见各篇。

---

## 阶段 6：网络协议栈与排障 ★（核心区）

> **对应笔记**：[[Linux/06_网络协议栈与排障/00_导读与知识地图|06 网络协议栈与排障]]（已按《写作规约》重写，共 9 篇）
>
> 本阶段是原理 4 篇（分层模型与数据包的一生 · 地址、路由与邻居 · 端口、套接字与多路复用 · 连接的生命周期）+ 体系 1 篇（一次请求的生命周期，含五条横切线）+ 实操 1 篇（协议栈观测与网络基线）+ 判例 1 篇 + 治理 1 篇。**入口是 [[Linux/06_网络协议栈与排障/00_导读与知识地图|06 网络协议栈与排障]]**，本页 checklist 按「到这里要能说清」验收。
>
> 知识主线是「**一次请求的生命周期**」六段：名字到地址 → 地址与出口 → 建链与两个队列 → 传输（窗口、拥塞、重传）→ 断开（`TIME_WAIT`/`CLOSE_WAIT`）→ 链路与转发。五条横切线是队列与上限、证据与计数、名字解析、主机内外、变更纪律。

- [ ] 原理 01（分层模型与数据包的一生）：四层各管什么、各自能出什么错、证据在哪；封装与解封装的方向（发是自上而下，排障是自下而上）；抓包里一行怎么逐段读；MTU 与 MSS 的归属与关系；四元组与两种 socket；网络命名空间与 veth 为什么是容器网络的积木
- [ ] 原理 02（地址、路由与邻居）：用前缀截断判断「在不在同一网段」；路由表的字段与最长前缀优先；`ip route get` 的 `via`/`dev`/`src`/`uid`；源地址是选出来的；`ip rule` 先选表后查表；邻居表五种状态的含义；`ip_forward` 与五链的处理顺序
- [ ] 原理 03（端口、套接字与多路复用）：端口三段划分与 **Linux 自己的源端口范围**（`ip_local_port_range`，不是 IANA 的临时端口段）；两种 socket 上 `Recv-Q`/`Send-Q` 的不同语义；状态集合与责任归属；源端口是一种有限资源（`QPS × 60 秒`）；两个队列与「客户端说连上了、服务端说没见过」
- [ ] 原理 04（连接的生命周期）：应用走 `getaddrinfo()` → NSS，`dig` 绕过它；`getent`/`resolvectl`/`dig` 三个视角；`search` × `ndots` 把一次解析放大成多次；三次握手与两个队列的长度由谁决定；`cwnd` 与 `rwnd` 取小者、`app_limited` 的含义；`cubic` 与 `BBR` 的判据差异；四次挥手、`TIME_WAIT` 的意义与不可调性、`CLOSE_WAIT` 只能改应用
- [ ] 体系 05（一次请求的生命周期）：六段主线一条线走完；每个字段的「是什么 / 单位 / 判据 / 与谁同看 / 转向哪一步」；转发路径上的四道关（转发开关、路由与邻居、`FORWARD` 链、conntrack）；五条横切线的收口
- [ ] 实操 06（协议栈观测与网络基线）：一个问题一把工具，分清**命令**与**内核接口文件**；`ss -s`/`ss -tanp`/`ss -lntp`/`ss -ti` 的分工与逐字段读法；`nstat` 的四种观察方式与「必须取差值」；链路层要换一组工具（`ip -s link`/`ethtool -S/-g`/`softnet_stat`/`interrupts`）；采样纪律与可直接复制的基线卡
- [ ] 判例 07（高频误判）：十四条四段式判例，覆盖「`dig` 通所以应用没问题」「`ping` 通所以网络没问题」「服务端没日志所以请求没到」「一次 `ss` 干净所以队列没满」「调 `tcp_fin_timeout` 治 `TIME_WAIT`」「`CLOSE_WAIT` 是内核参数问题」「只调 `somaxconn` 解决队列溢出」「关掉 `tcp_syncookies` 提性能」「吞吐上不去先扩带宽」「大包不通先扩带宽」「`conntrack -C` 是 0 所以健康」「主机侧干净继续调本机」「`ethtool -S` 计数器可跨机型复用」
- [ ] 治理 08（网络基线与变更治理）：上限配在哪一层（`sysctl` / 内核接口文件 / 模块与算法 / unit 属性 / 应用）与各自的影响范围、持久化位置；四类容量怎么算（源端口、conntrack 表、监听队列、名字解析）；网络基线卡、变更记录、处置记录、MTU/链路变更检查单四份产物模板；哪些故障必须靠计数器告警（日志里可能什么都没有）

> RDMA/RoCE 的无损网络配置（PFC/ECN/DCQCN、GID 索引、交换机侧配合）与 perftest 基线属于 AI 基础设施主题域，见 [[AI-Infra/00_简介|AI-Infra 大纲]] 的 [[AI-Infra/03_RDMA与RoCE无损网络]] 与 [[AI-Infra/04_perftest与性能基线]]。

**生产实践**

- 分层排查是基本功：能 `ping` 通不代表 TCP 能连（防火墙、`FORWARD` 链），TCP 能连不代表应用正常（DNS、证书、后端）
- **先分类再动手**：连不上先分「没监听 / 队列溢出 / 被丢 / 被拒」；变慢先分「应用没喂满 / 窗口受限 / 丢包重传 / MTU」
- 计数器一律**取差值**，且任何单一计数器都不能单独定性——伪造源地址的 SYN 实验里，只看一个计数就会得出相反结论
- 参数调整**先量化、一次只改一项、复测一个业务周期**，写清载体与回滚；`net.core.*` 与 `nf_conntrack_max` 是全局参数，只能在整机改
- 生产抓包必须限流限时落盘（`-c`/`-w`/`timeout`），抓完及时清理；偶发问题先在实验链路的隔离命名空间里用 `tc netem` 复现，再决定是否生产取证
- 改动 MTU、环缓冲、bond 模式、防火墙会短暂断流，必须排窗口、有带外入口、预先准备好回滚命令
- 云环境下先分清「主机内」还是「安全组/负载均衡/云网络」；主机侧计数器干净时不要在本机反复折腾

**要点自测**

- 大量 `TIME_WAIT` 是问题吗？什么情况下需要处理？为什么 `CLOSE_WAIT` 只能改应用？
- 用户说「服务连不上」，你的排查顺序是什么？每一步用什么命令、期待看到什么？
- 客户端偶发连接失败、服务端却没有日志，可能和什么有关？判据是哪两个字段与哪两个计数？
- `getent hosts` 和 `dig` 结果不一致说明什么？解析慢先量哪三个数？
- MTU 不一致的典型现象是什么？怎么验证（为什么必须带 `-M do`）？
- TCP 能连通但吞吐上不去，你会看哪些指标？`app_limited` 出现时该往哪个方向走？
- 抓包怎么抓才能既拿到证据又不影响生产？为什么「抓不到包」也是有效信息？
- `conntrack -C` 是 0 有哪两种完全不同的含义？怎么区分？
- 一份合格的网络基线卡至少包含哪几类字段？为什么必须记录「不存在的东西」？

---

## 阶段 7：权限、账号与安全加固

**入口**：[[Linux/07_权限与安全加固/00_导读与知识地图|07 权限、账号与安全加固]]（原理 4 篇 · 体系 1 篇 · 实操 1 篇 · 判例 1 篇 · 治理 1 篇，共 9 篇）

**这条线研究什么**：一次特权访问从「进程声称自己是谁」到被放行或拒绝、再到留下痕迹与收敛现场的全过程——内核按数值认人、PAM 决定这次登录算不算数、路径与权限位做第一层判定、ACL 与 `chattr` 在九个字符之外再加约束、SUID 与 capabilities 在 `execve` 时换身份或换特权、MAC 与挂载选项在内核里再仲裁一次、auditd 与五类基线让前面每一层变得可核查。要练出的判断是：面对「权限看着对却被拒」「多了一个提权点」「账号锁了还能进」这三类现象，先分清卡在哪一层、哪条通道，再按证据动手。

**过关里程碑**（不按天数）

1. **复述**：合上笔记画出「一次特权访问的十站」与四条横切线，说清三类现象（权限看着对却被拒 / 提权面异常 / 停用不彻底）的现场在哪；
2. **只读基线**：在一台机器上完成五类基线采集并留下账号台账卡与提权面基线卡（生产机上也可以做）；
3. **可回滚变更**：完成一次最小授权变更（去特权的服务能力、收紧的 sudoers 或 SSH 认证项），写出验证命令、回滚命令与回滚触发条件，并用生效值验收；
4. **演练与收口**：在可快照实验机上完成一次权限类演练（造一次分层拒绝或一次 MAC 拒绝），交出处置记录与一段可复用的 Runbook。

学习内容清单、生产实践与自测已并入章内各篇：清单与现场定位表见该章 `00_导读`，逐项内容与自测见各篇。

---

## 阶段 8：systemd、服务与定时任务

**入口**：[[Linux/08_systemd与服务管理/00_导读与知识地图|08 systemd、服务与定时任务]]（原理 4 篇 · 体系 1 篇 · 实操 1 篇 · 判例 1 篇 · 治理 1 篇，共 9 篇）

**这条线研究什么**：一个 unit 从磁盘上的一份配置文件，到成为 manager 认知里的生效配置、被编队与拉起、被判定「算不算启动完成」、被放进 cgroup 并换掉视野、运行中把证据留在 journald、退出后决定重来还是退场、最后决定开机时还参不参与的全过程。四条横切线贯穿七站：生效链（文件 → manager → 进程）、判据链（什么算开始、什么算结束）、边界线（上限与视野配在哪一层）、留痕线（证据在哪、还能留多久）。要练出的判断是：面对「改了配置没生效」「起来了却不可用」「反复重启后彻底不动」这三类现象，先说清卡在哪一站、依据是哪一份配置，再按证据动手。

**过关里程碑**（不按天数）

1. **复述**：合上笔记画出「一个 unit 的七站」与四条横切线，说清「改了配置没生效」「服务没起来」与「服务没就绪」三者的现场分别在哪一站；
2. **只读基线**：在一台机器上完成服务与定时任务基线采集（状态与失败单元、关键单元的生效文件与上限、自启方式、timer 与 cron、`/tmp` 清理语义、日志容量与时间基线），全程只读，生产机上也可以做；
3. **可回滚变更**：用 drop-in 完成一次最小变更，写成 `verify → daemon-reload → show 核对 → restart → 业务验证 → enable` 的固定顺序，每一步都给出验证方式与回滚命令，并用生效值与进程侧证据双重验收；
4. **演练与收口**：在可快照实验机上造一次启动失败或一次启动节流，交出分层定位记录与处置记录，分清哪一步是止血、哪一步是修复。

学习内容清单、生产实践与自测已并入章内各篇：清单与现场定位表见该章 `00_导读`，逐项内容与自测见各篇。

---

## 阶段 9：日志、监控与可观测性

> **对应笔记**：[[Linux/09_日志与监控/00_导读与知识地图|09 日志、监控与可观测性]]（已按「主笔记 + 子笔记」拆分，共 19 篇）
>
> 这一章有两条主线。**知识主线**是「一条日志的一生」（诞生 → 落点 → 轮转 → 集中 → 退役）与「一次告警的一生」（计量 → 判定 → 降噪 → 问责），另有四条横切线（取用顺序、统计口径、成本与容量、时间基线）。**技能主线**是 S1–S9 九项可交付能力，配 T0–T4 四级训练台阶。
>
> 日志链路与监控栈机制均为实测（Prometheus、Alertmanager、blackbox_exporter、node_exporter、sysstat 用发行版包**解包运行**）；正常部署、真正的跨机日志集中、日志平台、追踪后端、K8s 采集、云监控与 RHEL 默认值标注为「未实测」，详见各子笔记开头的「参考资料」。**读者定位**：会基本 shell、用过 `systemctl status` 与 `top`、会 `tail` 日志，但没系统想过「日志为什么落在这里、谁在切它、磁盘满是谁占的、告警为什么响/不响」。

- [ ] 前置地基（01–03）：四条证据链（日志/指标/追踪/变更）的分工与取用顺序；最小维度集（环境/服务/实例/版本）；墙上时钟与单调时钟、时区与 NTP 核查；日志/容量/指标/历史四类观测的命令地图与「记下命令、带口径」的纪律
- [ ] 一条日志的一生 · 诞生（04）：三种落点（stdout / 自有文件 / syslog 套接字）的取舍；可排障日志的最低字段（时间/级别/服务/实例/请求 ID/错误码）；为什么不用日志解析做业务指标
- [ ] 落点（05）：journald 与 rsyslog 的分工与双落点（`ForwardToSyslog`）；journald 字段（`_SYSTEMD_UNIT`/`_PID`/`PRIORITY`/`__MONOTONIC_TIMESTAMP`）；`Storage=`/`SystemMaxUse=`/`MaxRetentionSec=`/`RateLimit*` 与 `journalctl` 常用用法
- [ ] 轮转（06）：`daily`/`size`/`rotate`/`compress`/`delaycompress`/`notifempty`/`sharedscripts` 与状态文件；`copytruncate` 与 `create`+`postrotate` 的本质差别（inode 与数据落点的实测证据）；`-d` 与 `-v -f` 的使用边界
- [ ] 集中（07）：RFC 5424 报文逐字段（PRI = facility×8 + severity）与 `timeQuality`；UDP(`@`)/TCP(`@@`)/RELP/TLS 的取舍；rsyslog / Filebeat / Promtail / Vector 的选型；集中之后中心机的容量与单点风险
- [ ] 退役（08）：磁盘被写满的四类原因与判据；`df` 与 `du` 不一致、`/proc/<pid>/fd` 与 `lsof +L1`；三层保留策略（应用级别 + journald 容量 + logrotate 保留）；止血 → 治本 → 加防线的处置顺序
- [ ] 一次告警的一生 · 计量（09）：pull 模型与 `up`；关键 `node_*` 指标语义（`MemAvailable`、`filesystem_avail_bytes`、`sockstat_TCP_inuse`、`filefd_allocated`、采集器耗时）；counter 与 gauge 的用法；USE 检查表与 PSI
- [ ] 判定（10）：数据模型与 cardinality；四类 PromQL（`rate` / 比例 / 聚合分组 / 告警表达式）；recording rule 与 alerting rule；用 `ALERTS` 与 `promtool check rules` 验证；`for` 的取值
- [ ] 降噪（11）：路由与 `group_by`/`group_wait`/`repeat_interval`；抑制（`source_matchers`/`target_matchers`/`equal`）；静默（期限/原因/创建人）；降噪顺序 分组 → 抑制 → 静默；`blackbox_exporter` 的 `probe_success` 与证书到期（`probe_ssl_earliest_cert_expiry`）
- [ ] 问责（12）：SLI/SLO/错误预算与「几个 9」的换算；用错误预算决定发版节奏；告警分层（预算消耗速率 / 基础设施水位 / 单点抖动）与告警规范（命名、内容、路由、复盘）
- [ ] 横切 · 统计口径（13）：平均值掩盖长尾；同一份数据分位数四种口径的差异；全局分位数要靠合并直方图；瞬时采样漏掉的短脉冲与周期抖动；PSI 的 `some`/`full` 与累计 `total`
- [ ] 横切 · 成本与容量（14）：日志保留天数、指标 cardinality、追踪采样率三个数字与 owner；采集频率是线性成本；日志与业务数据分盘
- [ ] 横切 · 历史指标（15）：`sadc`/`sar`/`sadf` 的分工、`HISTORY=7` 与采集 timer、`sadf -d` 导出的是 UTC；「先确认有数据」的纪律
- [ ] 横切 · 分布式追踪（16，进阶）：trace/span 模型、W3C `traceparent` 与采样标志、头部与尾部采样；最小落地是上下文透传 + 把 `trace_id` 写进日志

**生产实践**

- 可观测性按四件套落地：日志（落点+持久化+轮转）、指标（每机采集+保留）、告警（`up`/水位/证书/错误率，每条有动作）、SLO（至少一个核心服务）；先闭环再扩指标
- 应用日志与系统日志分目录或分盘存放，避免日志写满根分区造成整机故障
- 保留策略三层同时配，缺一层都可能被打穿；日志集中只是把容量风险搬到中心机，中心机同样要有保留与告警
- 每台机器都要有可回溯的历史指标（`sar` 至少留两周），否则「昨天下午开始变慢」只能靠猜
- 告警要能对应到具体动作，并区分「立刻处理」与「排期处理」；只报不处理的告警等于没有告警
- 采集成本要有 owner：日志保留天数、指标 cardinality、追踪采样率三个数字写进方案；高基数信息进日志/追踪，不进 label
- 采集链路本身要可观测：`up`、抓取耗时、目标数量、日志采集器的队列/丢弃计数、`sar` 是否有当天数据
- 排障时先做只读取证，再把变更时间线与指标拐点对齐；时间未对齐前不下「谁先谁后」的结论

**交付物与要点自测**

- 交付物：一份可观测性基线卡、一份日志字段规范 + 落点清单、一份轮转配置 + 磁盘满处置记录、一份集中转发验收记录、一张指标检查表、一份规则文件 + CI 校验记录、一份告警规范 + 路由与抑制配置、一份 SLO 定义卡 + 错误预算台账、一份复盘报告 + 成本基线卡
- 磁盘被日志写满，你的处理顺序是什么？「删了文件但空间没释放」怎么办？
- 怎么用 `sar` 复盘「昨晚 3 点系统变慢」？为什么它常常「没有数据」？
- `logrotate` 的 `copytruncate` 有什么副作用？什么场景必须用它？`create` 之后为什么必须让应用重开文件？
- 为什么监控要看分位数而不是只看平均值？为什么「p95 是多少」还取决于分位数算法口径？
- 「服务变慢」和「服务报错」分别该先看哪条链路？为什么不能只靠日志？
- journald 与 rsyslog 的分工是什么？远程集中怎么选（`@` 与 `@@`、TLS、Filebeat/Promtail/Vector）？
- Prometheus 的「目标 → 规则 → Alertmanager → 通知」这条链路怎么逐段验证？`ALERTS` 与 `promtool` 各解决什么？
- 告警风暴的降噪顺序是什么？Alertmanager 的「抑制」和「静默」分别解决什么问题？
- SLI/SLO/错误预算怎么定义？它如何决定「哪些告警立刻处理、哪些排期」？
- 证书到期告警怎么写？`up` 和 `probe_success` 有什么区别？
- 为什么时钟不同步会让三条链路的证据拼不起来？

---

## 阶段 10：性能分析与故障排查 ★（核心区）

> **对应笔记**：[[Linux/10_性能分析与排障/00_导读与知识地图|10 性能分析与故障排查]]（已按「主笔记 + 子笔记」拆分，共 16 篇）
>
> 这一章有**一条知识主线**与**四条横切线**。知识主线是「**一次性能故障的处置六站**」：认领与量化 → 算力（CPU 与负载）→ 存储（内存与 IO）→ 传输与等待（网络与阻塞）→ 下钻（进程与栈）→ 止损、验证与复盘；四条横切线是观测代价与降级、视角缩放（整机/单核/进程/cgroup）、判据与统计口径、基线与容量。**技能主线**是 S1–S10 十项可交付能力，配 T0–T4 四级训练台阶。
>
> 单机 CPU / 内存 / IO / 网络 / 耗尽类实验，以及 cgroup v2 的 CPU 限流与内存 OOM 均为实测（`sysstat`、`strace`、`perf`、`fio`、`stress-ng` 用发行版包**解包运行**，不需要 root；网络注入放在 `unshare -U -r -n` 里，cgroup 用 `systemd-run --user`）；硬件 PMU、火焰图、off-CPU、eBPF、`blktrace`、`tcpdump`、真实网卡多队列、GPU 与多机摘流量/回滚标注为「未实测」，补做清单见子笔记 13。**读者定位**：会基本 shell、用过 `top`/`free`/`df`，但没系统想过「为什么整机 CPU 只有 4% 服务还是慢」「`%util` 没到 100% 算不算磁盘瓶颈」。

### 10.1 前置地基与处置六站（前半）

- [ ] 前置地基（01–03）：性能的五个可测量（延迟 / 吞吐 / 利用率 / 饱和度 / 错误）；排队效应与 Little's law（实测八组 `w/s × w_await` 与 `aqu-sz` 吻合，如 `324 × 0.00659 = 2.14` 对实测 2.14）；平均值掩盖长尾；整机 / 单核 / 进程线程 / cgroup 四个视角；按视角排布的命令地图与开销分级（L0 只读 → L4 注入）；观测者效应与采样偏差（`strace` 约 45 倍、`top` 首屏是短窗口差分而非自开机平均、`perf` 采不到 off-CPU）；工具不可用时的降级路径
- [ ] 第 1 站 · 认领与量化（04）：五个必答问题（谁受影响 / 多严重 / 从几点开始 / 还在不在 / 和什么比）；有 SLO 用错误预算、没有就用基线；对齐变更时间线与时钟；「慢」的三种形态（恒定 / 间歇 / 周期）决定采样密度；一次性取证脚本（实测输出 8208 字节，只读、生产机可跑）
- [ ] 第 2 站 · CPU 与负载（05）：`load` 含 D 状态且必须除以核数（实测单核 100% 时整机 `%usr` 仅 25.04%、`load` 仅 0.34）；`vmstat` 各列（注意 `si` 是换入、不是软中断）；`mpstat -P ALL` 找单核；`us` / `sy` / 软中断 / `wa` / `st` 五分支与各自的下钻工具；自愿与非自愿切换（实测 40,343.67 次/秒 vs 16.67）；同核超卖的非线性影响（吞吐 −79.0%，`p50` 8.13→8.06 ms、`max` 8.36→40.87 ms，整机仍仅 25.04%）
- [ ] 第 3 站 · 内存与 IO（06）：`MemAvailable` + PSI 判断「压力」而非「用量」（实测分配并触碰 3 GiB 后 PSI 全 0）；RSS/PSS/Anonymous；direct reclaim 与 THP；`iostat -x` 的 `await`/`aqu-sz`/`%util` 三者互不对应（实测 `%util=98.80%` 时 `aqu-sz=3.15`、`w_await=0.09 ms`，而 `%util=80.10%` 时 `aqu-sz=2.14`、`w_await=6.59 ms`）；`fio` 四象限基线；`fsync` 与缓冲写约 3400 倍；从设备 → 进程 → 文件 → 块层的下钻顺序与采样盲区

### 10.2 处置六站（后半）与横切篇

- [ ] 第 4 站 · 网络与等待（07）：三层证据（连接状态 → 协议栈计数器 → 收包侧丢弃）；重传率的算法与判据；`ss -ti` 的 `rtt`/`minrtt`/`cwnd`/`snd_wnd`/`app_limited`（实测注入 50 ms 单向延迟后 RTT 100.164 ms、`minrtt:100.131`）；`netem` 配 20% 丢包实测 35.5% 的算术解释（`1 − 0.8² = 36%`）；D 状态与 `wchan`；off-CPU 的三种替代路径；「计数器干净就不抓包」的决策线
- [ ] 第 5 站 · 下钻（08）：`/proc/<pid>` 的七个关键文件与「累计值必须差分」；`strace -c/-T/-f/-e trace=` 的分工与约 80 倍代价；`perf stat`/`record`/`sched` 的前提（无 PMU 时 `<not supported>`、`perf record -e cycles` 会采到 0 个样本、`perf sched` 被 tracefs 拒绝）；火焰图与 eBPF 的门槛与替代路径
- [ ] 第 6 站 · 止损、验证与复盘（09）：止损排序（限流 → 摘流量 → 回滚 → 扩容 → 重启）；每个动作的三件套（预期效果 / 验收指标 / 回滚方式）；「指标 + 业务 + 观察窗口」三件套验收；六段报告模板与实测示例；长期改进四个方向 + owner
- [ ] 横切 · 高频杀手场景（10）：十二类场景的指纹与最短判定路径（单核打满、锁竞争/过度唤醒、D 状态堆积、direct reclaim/THP、DNS 慢、日志同步落盘、备份抢 IO、端口耗尽、句柄耗尽、cgroup 限流、cgroup OOM、吞吐低但资源都不忙）
- [ ] 横切 · 容器与服务视角（11）：cgroup v2 关键文件；实测 `CPUQuota=20%` → `cpu.max=20000 100000`、`nr_throttled` 21→51、吞吐只剩 18.65%；实测 `MemoryMax=512M` → `memory.events` 出现 `oom_kill 1`、`memory.peak` 顶到 536870912、内核日志 `CONSTRAINT_MEMCG`；systemd 默认 `OOMPolicy=stop` 会让整个 unit 停掉；`systemd-run --user` 是无需 root 的练法
- [ ] 横切 · 基线与容量台账（12）：五组数据与采集条件（条件不同则数字不可比）；`fio` 四象限；历史指标「先确认有数据」（本机 `/var/log/sysstat` 根本不存在）；容量要按最热核与 p99 拐点算，不按整机平均算
- [ ] 横切 · 故障演练与工具边界（13）：三条纪律（隔离 / 上限 / 清理复核）；六类注入的观测指纹；缺依赖的标准处理（`ldd` → `apt-get download` → `dpkg-deb -x`）；工具可用性矩阵与未实测清单
- [ ] 收口（14–15）：按现象索引的处置手册（十二节 Runbook + 一页速查表）；十三个实验的索引与总自测
- [ ] 加速器与高速网卡的观测入口（本目录只给入口，不下钻）：`nvidia-smi dmon`、`rdma link`/`ibv_devinfo`、`ethtool -S` 的 pause 与错误计数（方法见[[AI-Infra/00_简介|AI-Infra 大纲]]）

**生产实践**

- 先建基线（业务高峰与低谷各一份），没有基线就没有「变慢了」；基线必须带采集条件（同一块盘的 `w_await` 在 0.09 ms 与 6.59 ms 之间摆动，条件不同不可比）
- 排障动作按「只读先于写入、应用层先于内核层、单机先于集群、限流先于重启」排序；一个动作只改一个变量；故障中不做高风险变更
- 每个止损动作先写下「预期效果 + 验收指标 + 回滚方式」；验收用「指标回到基线 + 业务验收 + 观察窗口」三件套，不用「重启后好了」
- 排障报告固定六段：时间线 / 影响面 / 证据 / 根因 / 止损动作 / 长期改进（含 owner 与期限；尤其要写「为什么之前的防线没拦住」）
- 用故障演练检验排障路径：单核超卖、内存压力、深队列 IO、网络延迟与丢包、句柄/端口耗尽、cgroup 配额；演练必须在隔离环境/命名空间里做、设定明确上限、跑完清理并复核，并记录观测指纹
- 容器与服务问题必须同时取容器内与宿主侧证据：限流看 `cpu.stat`、OOM 看 `memory.events`、IO 限速看 `io.max`；容器内 `top` 不代表容器的真实约束
- 工具方案要准备降级路径：无 PMU、无 tracefs、无 eBPF 时用 `/proc` 证据链 + 采样类工具 + 应用指标与分位数；缺依赖用解包补齐，不依赖安装

**交付物与要点自测**

- 交付物：一份性能基线卡、一份「按核 / 按线程」的定位记录、一张内存与 IO 判据卡、一次「先计数后抓包」的网络判定记录、一份带回滚与验收标准的止损记录、一篇六段排障报告、一份演练记录 + 工具可用性清单
- 负载飙高但 CPU 空闲，可能是什么原因？怎么一步步验证？
- 为什么「整机 CPU 4%」不能排除 CPU 问题？同核超卖对吞吐与延迟的实测影响是多少？
- CPU 高的五个分支怎么判、怎么下钻？`vmstat` 的 `si` 为什么容易看错？
- 内存「有压力」和「用得多」怎么区分？为什么分配 3 GiB 后 PSI 仍然全 0？
- `%util`、`await`、`aqu-sz` 为什么可能互不对应？怎么用 Little's law 交叉验证？
- `netem` 配 20% 丢包为什么实测 35.5%？
- 机器 IO 慢，你怎么把范围从「某块盘」缩小到「某个进程的某个文件」？
- `strace`/`perf`/eBPF 各有什么观测代价与前提？工具不可用时你的降级路径是什么？
- 容器的 CPU 被限流、内存被 OOM，分别在宿主侧看哪个文件？为什么容器内 `top` 不可信？
- 「偶发失败」类问题（连接 / 句柄 / 端口打满）怎么量化，并区分它与网络抖动的差别？
- 请把一次真实的排障讲给面试官听：现象 → 分层 → 证据 → 根因 → 止损 → 长期改进。

---

## 阶段 11：生产运维、变更与高可用

> **对应笔记**：[[Linux/11_生产运维与高可用/00_导读与知识地图|11 生产运维与高可用]]（已按「主笔记 + 子笔记」拆分，共 18 篇）
>
> 这一章有**一条知识主线**与**五条横切线**。知识主线是「**一次生产变更的一生**」五站：立项与评审 → 准备与预演 → 执行与分批推进 → 验证与守候 → 收口与沉淀；五条横切线是**可恢复性**（备份 → 恢复演练 → 灾难恢复）、**高可用与故障域**、**台账与基线**（资产/固件/参数/容量/证书）、**自动化与幂等**、**风险与窗口**。**技能主线**是 P1–P10 十项可交付能力，配 T0–T4 四级训练台阶；子笔记清单、技能地图与四级台阶都在主笔记里。
>
> 子笔记按难度分两层：标 **【进阶】** 的篇目（灾难恢复与救援入口）第一遍只需知道有这条路，等真遇到起不来、盘坏了再回来查；其余篇目属于本阶段必须掌握的核心。**实测状态**：`sysctl --system` 的加载顺序、`rsync --link-dest` 硬链接快照与恢复、`openssl s_client` 证书到期读取、`journalctl --disk-usage`、`df`/`du`/`lsof +L1`、**LVM 扩容与快照（在 loop 设备上演练，不动根分区）** 已在本实验机（Ubuntu 24.04.5 LTS / VMware 虚拟机 / 内核 `6.8.0-139-generic` / root 可用）跑过并给出实测数字；**未实测**：多机 VIP 切换与脑裂、Ansible 批量执行、restic/borg 备份恢复、云盘在线扩容、补丁窗口与重启批次、带外管理与救援介质——这些需要双机或真机环境，补做清单见子笔记 17。**读者定位**：已完成前十个阶段、会基本 shell 与资源排障，但没独立执行过一次生产变更。

### 11.1 前置地基与立项评审（01–04）

- [ ] 前置三条地基：什么叫「生产」（真实用户 + 真实数据 + 出错影响外部）、影响面的四个维度（用户 / 服务 / 数据 / 时间）、可逆性三档（只读 / 可回滚 / 不可逆）——并用只读命令（`ss -lnt`、`ss -tn state established`、`ip -brief addr`、`systemctl list-units --state=running`）在 3 分钟内判断一台机器是不是生产
- [ ] 变更分级：标准 / 重大 / 紧急三类的判据（影响多少用户、是否不可逆、能否快速回滚、有没有演练过）、冻结期与变更窗口、执行与验收的角色分离（执行人不给自己签字验收）
- [ ] 资产与依赖台账：硬件 / 固件 / 系统三组信息的采集命令（`lscpu`、`free -h`、`lsblk`、`dmidecode`、`lspci`、`ethtool -i`、`systemd-detect-virt`）；一阶依赖怎么用 `ss -lnt` + `ss -tnp` + `systemctl list-dependencies --reverse` 画出来；三类隐式依赖（定时任务、证书、采集代理）；配置漂移的发现方式
- [ ] 可恢复性的语言：RTO / RPO / MTTR / MTTD 各自约束什么；备份、快照、副本、归档四种东西各不能防什么；幂等与可重入的区别；验收三件套（指标 + 业务 + 观察窗口）
- [ ] 变更单六要素：目标、范围、备份、回滚、验证、窗口；写「范围」时要同时写「不改什么」，写「回滚」时要写「触发条件」而不只是命令
- [ ] 评审会必答四问：影响面多大、最坏结果是什么、怎么退回去（多久）、执行时谁有权喊停；紧急变更允许「先做后补」，但必须补单并复盘

### 11.2 准备、执行与收口（05–08）

- [ ] 变更前的三类备份对象（配置文件 / 数据 / 系统状态）与三条合格线（唯一的名字、不在被改动路径内、能 `diff` 与恢复验证）；`cp -a` 到带时间戳目录，并把状态快照（服务、监听、挂载、参数、启停清单）一并采集
- [ ] 回滚的四种形态（配置 / 版本 / 服务 / 数据）与两种假回滚（重启服务、换回文件但不 reload）；回滚命令必须在实验机上跑过并记下耗时，回滚触发条件必须写成「指标 + 阈值 + 持续时间」
- [ ] 灰度分批的四个维度（实例 / 故障域 / 流量 / 租户）与批次大小、观察时间的算法（批次不超过「总容量 − 最小可接受容量」留出的余量；观察时间覆盖一个业务周期与 2~3 个采样间隔）
- [ ] 执行顺序：摘流量（如需要）→ 改 → 单机验证 → 放回流量 → 观察；摘流量的验收是「从外部探失败、从本机探成功」；批次决策点四选一（继续 / 扩批 / 暂停 / 回滚），「解释不了但服务还活着」一律归为暂停
- [ ] 三层验收：单机（运行时值、`systemctl show`、新增报错）、集群（可用实例数、容量水位、冗余）、业务（真实业务路径，而不是只打 `/healthz`）；四种「假成功」（重启后恢复、没有告警、探活全过、跟以前一样）
- [ ] 收口四件事：归档记录（改前 + 改后 + 变更窗口日志）、更新台账与文档、闭环改进项、沉淀 Runbook；复盘六段（时间线 / 影响面 / 证据 / 根因 / 止损动作 / 改进项），并且**「人为失误」不算根因**
- [ ] 改进项写成「有动作、有 owner、有期限、有验收方式」的句子；三种去向：立即修、排期、显式接受风险（含批准人）

### 11.3 横切能力（09–15）

- [ ] 批量运维与自动化纪律（09）：批量 `ssh` 的连接层坑（连接放大、无超时、首连交互）与环境层坑（非交互 shell 不读 `.bashrc`、`PATH` 与别名不同）；Ansible 的 inventory / playbook / 幂等，`--check`、`--limit`、`serial`、`forks` 各自解决什么；脚本四件套（`set -euo pipefail`、`trap ... EXIT`、`timeout`、显式退出码）与 `flock` 防并跑
- [ ] 备份与恢复（10）：决定方案的四个问题（备什么、备到哪、多久一次、怎么验证）；三种粒度（文件级 / 块级 / 应用级）；运行中的数据库不能只靠复制文件——三条一致性路径；3-2-1 与「1 份离线或不可变」；恢复演练八步与实测 RTO
- [ ] 【进阶】灾难恢复（11）：四类灾难（系统起不来、根分区损坏、关键文件误删、整机不可用）各自的最短路径；「修还是重来」的两分钟判断；文件误删的黄金几分钟（停止写入、`lsof +L1`、从备份恢复，**不要先重启**）；带外管理与救援介质的提前准备清单
- [ ] 高可用与故障域（12）：六层故障域（进程 → 主机 → 机架 → 可用区 → 机房 → 共享依赖）与「拔掉一台业务还剩多少」的判定；VIP/VRRP 与脑裂、独立心跳链路与仲裁、fencing；健康检查判得太松与太严的后果；连接排空；N+1/N+2 余量与滚动重启批次的关系
- [ ] 容量、日志与目录治理（13）：`df` 与 `du` 不一致的四类成因；扩容是一条链（云盘：卷 → 分区 → 文件系统；LVM：`pvresize` → `lvextend -r`）与「扩容后必须看 `df`」；journald 的 `SystemMaxUse` 与 `logrotate` 的分工；临时文件治理的四个字段（owner / 保留期 / 上限 / 告警）；容器数据目录的三个增长来源
- [ ] 系统基线与参数治理（14）：`sysctl` 的加载顺序（目录合并按文件名排序、同名文件 `/etc` 优先、最后应用 `/etc/sysctl.conf`，**后加载覆盖先加载**）与「改了不生效」的五步排查；常见调优项的副作用（`somaxconn`、临时端口、`tcp_tw_reuse`、`fs.file-max` 与 `LimitNOFILE`、脏页回写）；参数台账的六个字段；BIOS/UEFI、RAID 卡与 BBU、网卡固件为什么要进台账，以及「虚拟化环境会伪造硬件信息」（GPU/网卡固件的深水区见 [[AI-Infra/00_简介|AI-Infra 大纲]]）
- [ ] 证书、补丁与合规（15）：证书四个时间点与到期检查命令（`openssl s_client -servername`，必须从外部连、必须按剩余天数告警）；证书链不全为什么表现为「部分客户端连不上」；内部 CA 轮换与 ACME 续期失败的常见原因；补丁三分类与「哪些补丁必须重启」；日志留存的三要素（时钟同步、不可篡改、到期才删）

### 11.4 收口（16–17）

- [ ] 能按处置手册定位十二类场景：变更失败要不要回滚、一批里部分成功、事故刚发生、发布后错误率上升、单机挂了流量没切、根分区满、参数不生效、证书告警、恢复备份失败、系统起不来、批量脚本留半成品、只有一个人会做
- [ ] 每类场景都能说出「第一反应不要做什么 → 证据 → 处置 → 验收 → 升级条件」，并写成一段 Runbook 片段
- [ ] 能背出事故通报模板的七行（现象 / 范围 / 开始时间 / 当前状态 / 已知原因 / 下一步 / 下次通报），并把「事实 / 推测 / 待确认」分开写

**生产实践**

- 所有变更遵循「先备份、再验证、后生效、可回滚」，并且把**回滚命令与触发条件**写下来——写在纸上、在实验机上跑过，才算有回滚方案
- 一次只改一个变量、一批只动一部分；灰度同时覆盖两个维度（先按实例验配置，再按故障域验依赖），每个批次都有明确的决策点
- 内核与系统参数按批次推进，先非核心业务；重启前确认上层集群或负载均衡能承接流量，并算清 N+1/N+2 余量
- 备份的唯一验收标准是「能恢复」：定期做恢复演练并记录耗时，对齐 RTO/RPO；**快照与副本都不算备份**
- 能自动化的手工操作尽量自动化，但脚本必须先满足四件套与幂等，再上生产；批量工具先 `--check`、再 `--limit`、最后分批
- 把「止损」和「根因」分开：事故中先恢复业务并保留现场，复盘阶段再追根因；带着「必须当场查清楚」的心态处置，往往会把故障拖长
- 把「有到期日的东西」全部列成一张表（证书、补丁、固件、支持合同、备份保留期），配上责任人和提前量足够（而不是到期当天）的告警
- 任何「只有我会做」的动作都要写成 Runbook 并让另一个人照着做过一次——否则它随时会变成下一次事故的放大器

**交付物与要点自测**

- 交付物：一份六要素齐全的变更单、一份带实测回滚耗时的变更记录、一份批量执行记录（含幂等检查结果）、一份三层验收记录（含回滚触发线）、一份带回滚与验收标准的止损记录、一份带 RTO 计时的恢复演练记录、一份资产与到期台账、一段 Runbook 片段、一篇含 owner 与期限的复盘
- 变更单六要素是什么？最容易漏的是哪两个？为什么「改动很小」不能作为降级的理由？
- 备份、快照、副本分别不能防什么？为什么说「备份的唯一验收标准是能恢复」？
- 3-2-1 原则是什么？为什么还要加「一份离线或不可变」？
- 要在一批 100 台机器上改一个参数，你的方案是什么？（先手工 1 台 → 幂等脚本/playbook → `--limit` 试跑 → `serial` 分批 → 每批验收 → 回滚条件）
- 灰度分批的依据是什么？批次大小与观察时间怎么算？为什么 `serial: 1` 不等于安全？
- 「拔掉一台业务还剩多少」怎么算？为什么「配了 VIP」不等于高可用？脑裂时应该保可用还是保一致？
- `df` 满了但 `du` 很少，四类原因是什么？分别怎么验证？
- 参数改了不生效，怎么在四步内定位？为什么 `/etc/sysctl.conf` 会让你的文件「失效」？
- 一台核心服务器根分区快满了，处置顺序是什么？哪三样东西不该先删？
- 证书为什么总是到线上才发现过期？到期管理要落哪四件事（台账、按剩余天数告警、续期失败告警、从外部验证）？
- 线上故障发生了，你的前三步是什么？什么时候才轮到查根因？通报里最容易犯的错是什么？
- 怎么向面试官证明你的备份有效、你的恢复能力不依赖某一个人？

---

## 阶段 12：复习与自测

> **对应笔记**：[[Linux/12_复习与自测/00_导读与知识地图|12 复习与自测]]（已按「主笔记 + 子笔记」拆分，共 16 篇 = 00 主笔记 + 15 子笔记）
>
> 这一章有**一条知识主线**与**五条横切线**。知识主线是「**一次复习闭环**」五站：建图（把 11 章收敛成体系）→ 自测定位（闭卷找出真正的薄弱点）→ 补缺深挖（缺哪一层补哪一层）→ 输出固化（一页纸、三分钟、脱稿）→ 迁移应用（场景题、限时答辩、追问）；五条横切线是**证据与口径**、**分层答题**、**边界与风险**（第一反应不要做什么）、**章节连接点**、**间隔重复节奏**。**技能主线**是 R1–R8 八项可交付能力，配 T0–T4 四级训练台阶；子笔记清单、技能地图与四级台阶都在主笔记里。
>
> **这一章不引入新知识**：全部题源与判据来自阶段 1–11，每道题都能在对应阶段找到出处；如果本阶段与对应阶段冲突，**以对应阶段为准**。引用纪律上，本阶段提及其他阶段一律用纯文本（如「阶段 6」），不跨章节加 wikilink；本章也不设「回到路线图」与「验证进度」。**读者定位**：已经跟完（或正在跟）阶段 1–11，会基本 shell 与资源排障，但存在「串不起来、看完就忘、能看懂说不出」三类问题的运维/SRE 新人。**实测状态**：本章引用的数字全部转引自前十一章在本实验机（**Ubuntu 24.04.5 LTS / VMware 虚拟机 / 内核 `6.8.0-139-generic` / systemd 255 / 4 vCPU / `MemTotal` 7894 MiB / root 可用**）的实测记录；需要双机的部分（VIP 切换、批量运维）与需要控制台或快照的自锁类演练（写坏 `fstab`、**把真正的根分区 `remount,ro`**、内核 panic）标注为未实测并给补做方式；**打满文件系统、耗尽 inode 与「只读文件系统」已用临时 loop 文件系统等价复现并实测**（见子笔记 12 的实验 3、4、12a）。

### 12.1 前置地基与建图（01–04）

- [ ] 复习的科学（01）：重读只带来「熟悉感」，复习要练的是**提取**；主动回忆的四种做法（闭卷写、白板画、讲给人听、闭卷做题）；间隔重复 **D+1 / D+3 / D+7 / D+21**；交错练习让「先分类」成为习惯；错题本**五列**（题目 / 当时的答案 / 正确口径 / 缺的那一层 / 复查日期），其中「缺的那一层」与「复查日期」最常被省掉
- [ ] 证据与口径（02）：一个数字要带**单位、版本、采集条件**三条尾巴；本套笔记统一容量用 MiB、时间用秒；证据 = 命令 + 输出形态 + 环境 + 时间；分位数要报口径（同一组样本的 p95 按四种算法实测为 **20 / 24 / 96 / 100 ms**）；同机不同窗口的 `w_await` 可以是 **0.09 ms** 也可以是 **6.59 ms**；说不清就显式写「未实测 + 原因 + 补做方式」
- [ ] 知识体系怎么搭（03）：一棵树（CPU / 内存 / IO·存储 / 网络四个资源根 + 权限与安全约束 + systemd 与交付组织层）；五条时间线；**九个连接点**（Page Cache、cgroup、时间同步、PID 1、容器三件套、句柄与端口、内核参数、登录与权限、日志落点）；每个知识点过「是什么 / 边界在哪 / 生产上怎么用」三问；画图三纪律（一图一问、箭头有方向、每框有命令）
- [ ] 建图（04）：半天画出三张图——**资源全景图**（机器上有什么）、**时间线图**（过程怎么走、在哪断）、**排障入口图**（出事先看哪一层）；并把十一个阶段各压成「一句话 + 一条最小证据命令 + 一个带条件的数字」；建图只允许翻术语表，卡住处标「？」交给下一站

### 12.2 自测、补缺与输出（05–08）

- [ ] 自测定位（05）：闭卷、限时、写下来，不会的写「空白」不猜；四档评分（**A 能脱稿讲 / B 有结论无证据 / C 认得但讲不出 / D 完全不会**），C 档最危险、按「完全不会」处理；薄弱排序 = 出现频率 × 影响面 × 不熟练度，**本轮只处理前 5 项**；第一次摸底建议只做阶段 2、4、5、6 四组
- [ ] 补缺深挖（06）：五种缺口（结论 / 判据 / 证据 / 边界 / 代价）各有各的补法；补缺的正确形态是「带着问题回原篇，只读 1~2 篇」；产出是**判据卡 + 一句「第一反应不要做什么」**，不是重抄笔记；补缺顺序先地基后横切——「越读越多」通常说明缺的是前置篇；必须记录「我原来错在哪」
- [ ] 输出固化（07）：一屏一页纸（现象 / 判据 / 命令 / 阈值 / 边界 / 不要做）；三分钟口述六段（现象与影响面 → 分层 → 证据 → 结论 → 止损 → 改进）并**结论先行**；脱稿不是背稿，而是能重建因果链；追问两层（「你怎么知道」与「换个条件呢」）必须都能接住；三种变形要避免：抄写型、菜单型、免责型
- [ ] 迁移应用（08）：迁移三个维度（换现象、换条件、换角色）；限时答辩 3 + 20 + 7 分钟，评分五要素（影响面 / 分层 / 证据 / 边界 / 止损与回滚，各 0~2 分）；追问链四步（证据 → 边界 → 反例 → 代价）；值班视角与面试视角的唯一差别是**先止损还是先讲清楚**；答不出时用「缩小范围 + 下一步验证什么」

### 12.3 横切能力（09–13）

- [ ] 跨章大图（09）：五条时间线并排（启动五环 / 进程一生 / 内存旅程 / 盘的一生 / 连接六站，另加日志与告警两条链路）与**九个连接点**的「两个视角 → 如何对齐 → 最容易答错的地方」；三张跨域图分别收口在连接层、内存层、变更层
- [ ] 场景题答题骨架（10）：十五道场景题（负载高但 CPU 不高 / 内存增长直到 OOM / 磁盘满或 inode 满 / 句柄与进程数耗尽 / 服务连不上 / 偶发延迟丢包 / 服务起不来或定时任务没跑 / 疑似入侵 / 内核 panic / 根分区只读 / 单核打满 / 资源空闲但变慢 / SSH 慢 / 时间漂移 / 容器 OOMKilled）各一份六段骨架，每题至少准备**两个成因**和一句「第一反应不要做什么」
- [ ] 高频易错点榜（11）：八十四组「常见说法 vs 事实」，覆盖内存与资源、进程与信号、存储与 IO、网络、systemd、权限与安全、启动与内核、可观测性与生产八组；用法是**遮住右列自测**并统计每组的错误数，把出错的行抄进错题本
- [ ] 端到端演练（12）：十二个实验编成一条链（只读基线 / `fstab` / 打满文件系统 / 耗尽 inode / cgroup OOM / CPU 限流 / 句柄耗尽 / 单核打满 / 日志轮转写错 / 网络丢包 / 服务起不来 / 只读根与 panic，其中实验 12 拆为 12a 只读文件系统与 12b 真正的根分区只读及 panic 两档）；三条纪律（隔离 / 上限 / 清理复核）；每个实验必须留下**观测指纹**与**恢复耗时**；**按可自恢复性分级**：只读与可回滚的已实测（打满文件系统、耗尽 inode 与「只读文件系统」用临时 loop 文件系统等价复现），需要控制台或快照的（写坏 `fstab`、把真正的根分区 `remount,ro`、内核 panic）标注未实测并给补做方式
- [ ] 向外连接（13）：容器的三个宿主侧机制（namespace / cgroup / 根文件系统与联合挂载）；Linux 概念 ↔ 容器与编排概念 ↔ 宿主侧证据的对照表；三个高频追问的宿主侧答法（OOMKilled / CPU 被限流 / 网络不通）；云上 Linux 的三层变量（虚拟化、云盘与网络、平台配额，含共享型实例的 CPU 积分）

### 12.4 收口（14–15）

- [ ] 能按现象处置复习盲区：串不起来、看完就忘、每次答案不同、有结论无证据、数字口径混乱、被追问就崩、原理会但给不出命令、场景题没头绪、会讲不敢动手、复习断档、只剩三天、值班前热身、容器题答不上——每类都能说出「缺的是什么 → 去哪一篇 → 怎么练 → 怎么验收」
- [ ] 五件交付物齐备并各有一份验收记录：**体系图、错题本、一页纸、限时答辩记录、端到端演练记录**
- [ ] 六十三道闭卷题（按十一个阶段分组）全部做过一轮，错题进入错题本并排出 D+1 / D+3 / D+7 / D+21
- [ ] 原「核心要点速查」的十一组要点逐条能在题库或坑榜里找到落点——含 LVM 扩容、RAID 取舍、SSH 加固、sar 复盘、USE 方法、trace 与 span、火焰图，不允许在重写中丢点

**生产实践**

- 复习的时间花在「取」上而不是「看」上：每读一节就合上书写 3~5 行，每次复习都要有产物
- 所有结论带单位、版本与采集条件；本套笔记统一容量 MiB、时间秒；说不清的部分显式标「未实测」
- 答题固定六段骨架（现象与影响面 → 分层 → 证据 → 结论 → 止损 → 改进），并保证每题至少两个成因、一句「第一反应不要做什么」
- 复习动作也遵守生产纪律：T1 只读可在生产机做，T2 以上只在实验机；不做未审批的「验证性变更」，不拿唯一的备份做恢复演练
- 错题按 D+1 / D+3 / D+7 / D+21 复查，只碰「上次标错的」与「快忘的」；每条错题都要指到一篇子笔记
- 排期按三周推进（建图与摸底 → 补缺与输出 → 演练与限时答辩），允许砍内容但不允许砍交付物；每天不超过 3 段 × 25 分钟
- 容器与云上的问题一律「宿主侧 + 容器侧」双视角取证，不把问题推给编排层或平台

**交付物与要点自测**

- 交付物：一张自己画的体系图（资源全景 / 时间线 / 排障入口至少两张）、一本五列错题本（每条含复查日期）、一张一屏一页纸、一份限时答辩记录（含四步追问与自评分数）、一份端到端演练记录（含观测指纹、恢复耗时、清理复核与未实测清单）
- 为什么「看了三遍」还是会忘？主动回忆与间隔重复分别在解决什么问题？
- 一个数字要带哪三条尾巴？同一组样本的 p95 为什么可以是 20 ms 也可以是 100 ms？
- 五条时间线是什么？九个连接点里你能说出哪三个？每个连接点的「最容易答错」是什么？
- 四档评分中哪一档最危险？薄弱排序的三个维度是什么？本轮应该处理几项？
- 五种缺口分别怎么补？为什么「补缺越读越多」通常说明缺的是前置篇？
- 一页纸与三分钟口述的骨架分别是什么？为什么口述要「结论先行」？
- 追问链四步是什么？值班视角与面试视角唯一的差别是什么？
- 十五道场景题里，任抽一道，你能不能说清「分层 → 证据 → 结论 → 止损 → 第一反应不要做什么」？
- 演练的三条纪律与三项清理复核是什么？观测指纹和恢复耗时为什么必须记录？
- 容器被 OOMKilled、CPU 被限流、网络不通时，宿主侧分别看什么？云上 Linux 多了哪三层变量？
- 怎么向面试官证明你在复习上是「有体系」的，而不是「背过多」的？（答：交付物 + 证据 + 边界，而不是结论数量）

---

## 推荐资源

- `man` 页与发行版官方文档：RHEL 文档、Ubuntu Server Guide、Debian Handbook（版本差异以它们为准）
- 《Linux 性能优化实战》（倪朋飞）：排障与性能分析部分最贴合运维面试
- 《Systems Performance》（Brendan Gregg）与其 USE 方法、FlameGraph 项目
- LWN.net（内核机制与新特性）、kernel.org 的 Documentation/
- 《UNIX/Linux 系统管理手册》（Nemeth 等）：体系化的系统管理视角
- 演练环境：本地 KVM/VM 或按量云主机、Killercoda 场景；破坏性实验只在实验机做
- 各发行版的 release notes：讨论版本差异时的依据，避免凭记忆答题
- Prometheus / Alertmanager 与 OpenTelemetry 官方文档：设计指标、告警与链路追踪时的依据
- `man` 之外的子系统文档：`tc`/`netem`、`nft`、`systemd.*` 子手册页与 kernel.org 的网络文档（阶段 6 与阶段 10 用得最多）

---

## 笔记组织建议（可选）

建议后续把每个阶段拆成独立笔记，编号与本文档一致（阶段笔记名刻意与 Kubernetes 笔记区分，避免 wikilink 混淆）：

- `Linux/00_学习大纲.md`（本文档，作为整套笔记的「地图」，不占用阶段编号）
- [[Linux/01_实验环境与操作纪律/00_导读与知识地图|01 实验环境与操作纪律]]
- [[Linux/02_启动流程与内核/00_导读与知识地图|02 启动流程与内核]]
- [[Linux/03_进程与信号/00_导读与知识地图|03 进程与信号]]
- [[Linux/04_内存管理与OOM/00_导读与知识地图|04 内存管理与 OOM]]
- [[Linux/05_存储与文件系统/00_导读与知识地图|05 存储与文件系统]]
- [[Linux/06_网络协议栈与排障/00_导读与知识地图|06 网络协议栈与排障]]（重点）
- [[Linux/07_权限与安全加固/00_导读与知识地图|07 权限、账号与安全加固]]
- [[Linux/08_systemd与服务管理/00_导读与知识地图|08 systemd、服务与定时任务]]
- [[Linux/09_日志与监控/00_导读与知识地图|09 日志、监控与可观测性]]
- [[Linux/10_性能分析与排障/00_导读与知识地图|10 性能分析与故障排查]]（重点）
- [[Linux/11_生产运维与高可用/00_导读与知识地图|11 生产运维与高可用]]
- [[Linux/12_复习与自测/00_导读与知识地图|12 复习与自测]]
- **模板已废弃**：新篇直接按 [[Linux/写作规约|写作规约]] 第三节的骨架写，实例见阶段 4（原理 / 体系 / 实操 / 判例 / 治理）。

> **跨目录说明**：本目录的学习地图是 `00_学习大纲.md`；Kubernetes 与 AI-Infra 的学习地图是 `00_简介.md`。跨目录引用一律用路径写法（如 `[[AI-Infra/00_简介]]`），避免 Obsidian 解析到别的目录。
>
> 目前已拆出 [[Linux/01_实验环境与操作纪律/00_导读与知识地图|01 实验环境与操作纪律]]（阶段 1）、[[Linux/02_启动流程与内核/00_导读与知识地图|02 启动流程与内核]]（阶段 2）、[[Linux/03_进程与信号/00_导读与知识地图|03 进程与信号]]（阶段 3）、[[Linux/04_内存管理与OOM/00_导读与知识地图|04 内存管理与 OOM]]（阶段 4）、[[Linux/05_存储与文件系统/00_导读与知识地图|05 存储与文件系统]]（阶段 5）、[[Linux/06_网络协议栈与排障/00_导读与知识地图|06 网络协议栈与排障]]（阶段 6）、[[Linux/07_权限与安全加固/00_导读与知识地图|07 权限、账号与安全加固]]（阶段 7）、[[Linux/08_systemd与服务管理/00_导读与知识地图|08 systemd、服务与定时任务]]（阶段 8）、[[Linux/09_日志与监控/00_导读与知识地图|09 日志、监控与可观测性]]（阶段 9）、[[Linux/10_性能分析与排障/00_导读与知识地图|10 性能分析与故障排查]]（阶段 10，重点）、[[Linux/11_生产运维与高可用/00_导读与知识地图|11 生产运维与高可用]]（阶段 11）与 [[Linux/12_复习与自测/00_导读与知识地图|12 复习与自测]]（阶段 12）；至此全部 12 个阶段的笔记均已拆出，不再有未解析的阶段链接。本文档作为这套笔记的「地图」，每个阶段学完后回来勾选 checklist、补充踩坑记录。
