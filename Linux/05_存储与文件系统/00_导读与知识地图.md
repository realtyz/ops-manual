---
tags:
  - Linux
  - 存储
  - 文件系统
  - IO
  - 学习地图
created: 2026-09-20
---

# 存储与文件系统

## 知识地图

### 一块盘的服役一生

时间主线跟着**一块盘**走：从被机器认出来，到拥有分区与文件系统，到挂进目录树开始服役，到容量长大，最后到坏掉、可控地换走。

```mermaid
flowchart LR
  A["① 接管<br/>认盘 · 分区 · 记台账"] --> B["② 成型<br/>选型 · mkfs · 验挂"]
  B --> C["③ 上岗<br/>挂载 · fstab · 验收"]
  C --> D["④ 长大<br/>池 · 在线扩容 · 快照"]
  D --> E["⑤ 冗余与退役<br/>RAID · 健康 · 换盘"]
```

| # | 站点 | 内核在做什么 | 到这里要能说清 |
| --- | --- | --- | --- |
| 1 | 接管 | 识别块设备、读盘头元数据、写分区表 | 为什么设备名靠不住；动手前的三问各自回答什么 |
| 2 | 成型 | 在新账本下建立 inode、块、日志等结构 | 选型的三问；哪些参数定了就改不了 |
| 3 | 上岗 | 把文件系统的根接到目录上，并由 systemd 保持 | 挂载为什么遮住原内容；哪两个挂载选项是保命的 |
| 4 | 长大 | 在块设备与文件系统之间插一层映射 | 从「盘变大」到「业务可用空间变大」要经过哪几层 |
| 5 | 冗余与退役 | 用冗余数据继续服务，并重建缺失成员 | 重建窗口为什么是风险；换盘最容易在哪三步出事 |

### 三条横切线

| 横切线 | 贯穿内容 | 收口于 |
| --- | --- | --- |
| 账目线 | 容量口径 → inode → 保留块 → 配额 → 已删除但被持有的文件 → 被挂载覆盖的目录 | 「`df` 与 `du` 对不上」怎么拆成四类 |
| 限制线 | 块与 inode → 保留块 → 配额 → 池的数据与元数据水位 → 句柄与事件监控上限 | 为什么「还有空间」却写不进去 |
| 完整性线 | 页缓存与回写 → 落盘语义 → 日志重放 → 只读保护 → 介质健康 → 阵列降级 | 哪些动作不可逆、顺序是什么 |

## 术语速查

| 名词 | 一句话解释 | 在哪一篇展开 |
| --- | --- | --- |
| 块设备（block device） | 能按固定大小的块随机寻址的存储，盘、分区、阵列、逻辑卷都是它 | 原理：块设备、分区与池 |
| 分区表 / GPT / MBR | 记录「这块盘被切成几段」的元数据；GPT 是现代默认，MBR 单盘上限 2 TiB | 原理：块设备、分区与池 |
| 设备名漂移 | 同一块盘在换插槽、加盘或云盘重挂后，名字可能从 `sdb` 变成 `sdc` | 原理：块设备、分区与池 |
| PV / VG / LV | 物理卷 / 卷组 / 逻辑卷，卷管理器的三层结构 | 原理：块设备、分区与池 |
| 精简置备（thin provisioning） | 允许卷声明空间大于池实际容量的做法，池写满会波及全组 | 原理：块设备、分区与池 |
| RAID / 重建 | 多盘冗余阵列 / 用冗余数据恢复缺失盘内容的过程 | 原理：块设备、分区与池 · 体系 |
| URE | 厂商标称的不可恢复读错误率，直接决定重建失败概率的量级 | 原理：块设备、分区与池 |
| 块 / 块大小 | 文件系统分配空间的最小单位；建文件系统时定下，之后改不了 | 原理：文件系统的结构与选型 |
| inode | 文件的元数据（权限、属主、时间、链接数、块指针），不含文件名 | 原理：文件系统的结构与选型 |
| 目录项（dentry） | 「文件名 → inode 号」的映射；名字存在这里 | 原理：文件系统的结构与选型 |
| 超级块 | 文件系统总账；备份超级块是救援入口 | 原理：文件系统的结构与选型 |
| 日志（journal） | 元数据变更的流水账，崩溃后靠重放恢复一致性 | 原理：文件系统的结构与选型 |
| 分配组（AG） | XFS 把空间切成的独立管理单元，支持并行分配，也是它不能缩容的原因 | 原理：文件系统的结构与选型 |
| 表观大小 / 实际占用 | 文件声称的字节数 / 真正分配的块数；稀疏文件上两者差别很大 | 原理：块设备、分区与池 · 实操 |
| 保留块 | ext4 默认留给 root 的那部分空间，普通用户写不进去 | 原理：容量边界、挂载与可见性 |
| 配额 / 项目配额 | 给用户、组或**目录树**设的容量上限；目录级边界用项目配额 | 原理：容量边界、挂载与可见性 |
| `ENOSPC` / `EDQUOT` / `EROFS` | 三种不同的 errno：资源用尽 / 配额到顶 / 只读 | 原理：容量边界、挂载与可见性 |
| 句柄（fd） | 进程打开文件或套接字的编号，有会话、服务、系统三级上限 | 原理：容量边界、挂载与可见性 |
| inotify | 文件事件监控机制，有 watch 数、实例数与事件队列三道上限 | 原理：容量边界、挂载与可见性 |
| 绑定挂载（bind mount） | 把一个已有目录再挂到另一个位置，同一份内容两个入口 | 原理：容量边界、挂载与可见性 |
| Page Cache / 回写 | 用空闲内存缓存文件内容 / 脏页稍后异步落盘的过程 | 原理：写入路径与完整性 |
| `fsync` | 要求内核把数据落到设备的动作；不调用就只有缓存保证 | 原理：写入路径与完整性 |
| `errors=remount-ro` | 出错即把文件系统重挂只读；**ext4 默认是出错继续** | 原理：写入路径与完整性 |
| 干跑检查 | 只报告不修改的检查（如 `e2fsck -n`、`xfs_repair -n`） | 原理：写入路径与完整性 · 实操 |
| `await` / `aqu-sz` / `%util` | 平均完成时间（含排队）/ 平均队列深度 / 设备忙的时间比例 | 实操 |
| `svctm` | 已废弃的列，工具文档已标注不可靠，不要用它下结论 | 实操 |
| 介质健康计数 | 重映射、待处理扇区、寿命消耗等；看**趋势**而不是单值 | 体系 · 实操 |
| 硬挂载 / 软挂载（`hard`/`soft`） | 网络存储对端不可达时无限重试（进程卡住）/ 超时返回错误（可能数据不一致） | 原理：写入路径与完整性 |
| 多路径（multipath） | 同一存储的多条物理路径聚合与故障切换；切换瞬间有秒级 IO 停顿 | 原理：写入路径与完整性 |
| 回收通知（TRIM） | 文件系统告诉闪存盘「哪些块已无效可擦除」的动作 | 原理：写入路径与完整性 |
| `data_percent` / `metadata_percent` | 池的数据与元数据水位，两个都必须监控 | 体系 · 治理 |

## 故障现场定位表

排障的第一问不是「怎么修」，而是「**这块空间算在谁账上、拒绝写入的是哪一类上限、这条链路的哪一层有问题**」。用这张表把现象定位到类别，再进对应篇目。

| 你看到的现象 | 属于哪一类 | 现场在哪 | 第一反应不要做 |
| --- | --- | --- | --- |
| 写入报 `No space left on device`，容量还有余 | inode 或保留块 | 容量与 inode 两个使用率、文件系统的保留块字段 | 不要因为「还有空间」就否掉磁盘方向 |
| 写入报 `Disk quota exceeded` | 配额 | 配额报表、软硬限与宽限期状态、挂载选项里的记账开关 | 不要和 `No space left on device` 混为一谈 |
| 删了大文件，用量不降 | 已删除但仍被持有 | 进程的打开句柄清单 | 不要反复删同一个文件，也不要先重启整机 |
| `df` 与 `du` 差了几十 GiB | 四类不一致 | 被持有的删除文件、被覆盖的目录、保留块与元数据、稀疏文件 | 不要说「某个工具不准」就结束 |
| 挂载后目录里原有数据「消失」 | 被挂载覆盖 | 从上层绑定挂载查看 | 不要以为数据被删了，更不要在里面重建同名文件 |
| 目录「应该」在独立盘上，其实不在 | 归属假设未验证 | 该路径实际属于哪个文件系统 | 不要用目录名推断文件系统归属 |
| 文件系统突然变只读 | 内核保护动作 | 内核日志与硬件健康计数 | **不要重挂为可写** |
| 内核日志出现介质错误 / IO 错误 | 硬件先兆 | 内核日志、健康关键计数 | 不要先重启（现场会被清掉） |
| 阵列降级 | 冗余丢失 | 阵列状态与成员标记 | 不要立刻拔盘换新 |
| 设备完成时间很高 | 排队或设备退化 | 完成时间与队列深度的组合、请求大小、读/写分开看 | 不要只看一个列就宣布饱和或换盘 |
| 写入变慢、进程进不可中断睡眠 | 回写积压或存储链路 | 脏页与回写量、进程级 IO、网络存储参数 | 不要跳过取证直接重启 |
| 整机容量命令卡住、大量进程不可中断睡眠 | 挂载点卡死 | 内核日志、多路径状态、网络可达性 | 不要批量强杀进程 |
| 网络存储对端抖动，写请求长时间无响应 | 挂载语义 | 挂载选项与超时重传参数、对端与网络状态 | 不要为了「不卡」把写场景改成软挂载 |
| 闪存盘写入变慢、寿命告警 | 回收与磨损 | 回收任务的启用状态、寿命消耗与备用块余量 | 不要把它当容量问题，也不要只看单次寿命读数 |
| 重启后进紧急模式、报挂载失败 | 挂载配置 | 失败的挂载单元与其日志 | 不要重装 |
| 云盘扩容了但业务容量没变 | 链路只做了一半 | 设备 → 分区 → PV → VG → LV → 文件系统逐层核对 | 不要跳过中间层直接扩文件系统 |
| 共享目录被一个租户写满 | 缺边界 | 目录用量排行、配额报表 | 不要只做事后清理 |
| 应用报 `too many open files` 或监控漏事件 | 非空间的容量上限 | 进程实际生效的限制、系统级计数与事件监控上限 | 不要只调大上限而不看应用设计 |
| 健康计数出现非零值 | 需要看趋势 | 同一计数的历史序列 | 不要用单次数值决定换盘 |

两个容易被忽略的边界：

- **「够不够」与「谁在用」是两个问题。** 前者看账本（容量、inode、配额、池水位），后者看目录树与进程。混在一起问，就会得出「还有 40% 怎么可能写不进去」这类结论。
- **「变慢」与「写坏」是两种结局。** 回写积压、排队、重建都制造变慢（可观测、可调优）；只读、介质错误、阵列降级制造数据风险（先取证，再动手）。**修复类动作大多不可逆**，这是处置顺序必须固定的原因。

## 篇目

| # | 篇目 | 一句话 |
| --- | --- | --- |
| 01 | [[Linux/05_存储与文件系统/01_原理_块设备分区与池\|原理：块设备、分区与池]] | 内核怎么认识一块盘、设备名为什么靠不住、池换来了什么 |
| 02 | [[Linux/05_存储与文件系统/02_原理_文件系统的结构与选型\|原理：文件系统的结构与选型]] | inode、块、日志与 ext4/XFS 的结构差异 |
| 03 | [[Linux/05_存储与文件系统/03_原理_容量边界挂载与可见性\|原理：容量边界、挂载与可见性]] | 「还有空间」却写不进去的七种机制 |
| 04 | [[Linux/05_存储与文件系统/04_原理_写入路径与完整性\|原理：写入路径与完整性]] | 写返回不等于落盘；出错时内核在保护什么 |
| 05 | [[Linux/05_存储与文件系统/05_体系_一块盘的服役一生\|体系：一块盘的服役一生]] | 五站主线与三条横切线，一篇讲完整条链 |
| 06 | [[Linux/05_存储与文件系统/06_实操_观测与基线\|实操：观测与基线]] | 用哪把工具、读哪个字段、怎么留基线 |
| 07 | [[Linux/05_存储与文件系统/07_判例_高频误判\|判例：高频误判]] | 现象 → 误判 → 判据 → 出处 |
| 08 | [[Linux/05_存储与文件系统/08_治理_容量与限制治理\|治理：容量与限制治理]] | 上限配在哪一层、产物模板与行业做法 |

## 参考文档

- `man 8 lsblk`、`man 8 blkid`、`man 8 findmnt`、`man 8 mount`、`man 8 umount`、`man 5 fstab`、`man 5 systemd.mount`、`man 5 systemd.automount`。
- `man 8 parted`、`man 8 fdisk`、`man 8 sfdisk`、`man 8 partprobe`、`man 8 wipefs`、`man 4 sd`。
- `man 8 mke2fs`、`man 8 tune2fs`、`man 8 dumpe2fs`、`man 8 e2fsck`、`man 8 debugfs`、`man 8 resize2fs`。
- `man 8 mkfs.xfs`、`man 8 xfs_info`、`man 8 xfs_growfs`、`man 8 xfs_repair`、`man 8 xfs_admin`、`man 8 xfs_quota`、`man 5 xfs_quota`。
- `man 8 lvm`、`man 8 pvcreate`、`man 8 pvresize`、`man 8 vgextend`、`man 8 lvextend`、`man 8 lvs`、`man 8 vgcfgbackup`。
- `man 8 mdadm`、`man 5 mdadm.conf`、`man 4 md`、`man 8 smartctl`、`man 8 nvme`、`man 8 fstrim`、`man 8 blkdiscard`。
- `man 5 nfs`、`man 8 mount.nfs`、`man 5 exports`、`man 8 nfsstat`、`man 8 iscsiadm`、`man 8 multipath`、`man 5 multipath.conf`、`man 5 systemd.mount`、`man 5 systemd.automount`。
- `man 1 df`、`man 1 du`、`man 1 stat`、`man 1 lsof`、`man 1 iostat`、`man 1 pidstat`、`man 8 iotop`、`man 1 fio`。
- `man 5 proc`、`man 5 sysfs`、`man 7 inotify`、`man 5 limits.conf`、`man 3 errno`。
- 内核文档 `Documentation/admin-guide/blockdev/`、`Documentation/filesystems/ext4/`、`Documentation/filesystems/xfs/`、`Documentation/block/`、`Documentation/admin-guide/device-mapper/`、`Documentation/admin-guide/sysctl/fs.rst`。
- 发行版官方文档中的存储管理、文件系统与配额章节；存储厂商文档中的介质错误与重建说明。

入口：[[Linux/00_学习大纲|00 学习大纲]]
