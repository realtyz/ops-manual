
**本文件是本目录内容写作与结构的唯一依据。** `Linux/写作规约.md` 的写法可以借鉴，但不作为本目录的规则——两个领域的知识对象不同（见第二节），规则必须各自成文。

本目录的写作规则、12 章结构、版本舞台表与同步清单收在本文件；仓库总览见根 `README.md`。

## 一、定位与读者

- 定位：面向运维 / SRE 的生产判断力——把"生产现场真正会被追问的点"和"现场真正会遇到的故障"对齐。验收标准是**能不能在事故现场用出来**，不是能不能复述概念。
- 读者：会基本 Shell 与 `kubectl`，用过 `systemctl`、`top`、`docker`，但没有系统学过分布式控制面；容器与编排的机制由本目录负责补。
- 各章各篇都不写"这一章写给谁""需要什么基础""读完能回答什么"，也不出现应试口径（"脱稿讲 3~5 分钟""错题本""正确率""面试加分"）。需要限定推演时写"限时推演"，需要说追问时写"值班口径"。

## 二、知识体系怎么组织

### 主线：收敛链

Linux 的知识对象是**一台机器上的一个机制**（内核怎么处理它），证据在 `/proc`、`sysctl`、man。Kubernetes 的知识对象是**一条声明在分布式系统里怎么被收敛成事实**，证据在 API 契约、`status` 与 `conditions`、事件、控制器日志、节点侧工具。

因此每章内部统一跟**一条收敛链**走：

> 声明（API 契约：字段与准入把它变成什么）→ 谁在 watch 它 → 收敛动作（控制器 / 调度器 / kubelet 各做什么）→ 可观测事实（`status` / `conditions` / 事件）→ 代价与失败形态

每个领域都有自己的"一条声明"：工作负载是 Pod 声明，网络是 Service / EndpointSlice 声明，存储是 PVC 声明，安全是 Subject 与 Rule 声明。

**由这条主线派生的三条硬规定**：

1. **每条收敛链在"体系"层至多一篇，且必须是这条链的完整展开**；压缩成多篇短篇会让链条断裂。一章里存在多条链时（例如网络既有 Service 链也有入口链），每条链各有一篇体系篇；本章以哪条链为主，写在结构表的"主链"一列里。
2. **运维章的主链不是声明链**：可观测性与生产运维的知识对象是"一次告警到复盘"的时间线，按"看清 → 判定 → 改变 → 回退"展开，但不得因此省掉代价与失败形态。
3. **一个动作牵动几处，就要在几处分别给判据**：改一次 `requests` 会同时牵动调度、QoS 档位、驱逐顺序、配额、HPA/VPA，分布在不同的章就各写各的，不能只在一处写。

### 分章：十一章正文 + 第 12 章收口

章号就是阅读顺序；**每章只依赖它前面的章**。依赖列写的是"进入本章之前应当已经读过哪些章"，这个指向只出现在本表里，正文与导读都不写（见第三节），因此"按章号顺序读"成立。

| 章   | 章名           | 主链                     | 一句话定位                                                                            | 依赖       |
| --- | ------------ | ---------------------- | -------------------------------------------------------------------------------- | -------- |
| 01  | 容器技术         | 一份制品到一组进程              | 一份制品（镜像）到一组进程之间那层技术栈：OCI 镜像、分发与供应链、联合挂载与快照器、运行时分层与 CRI                           | 无        |
| 02  | 核心架构与对象模型    | 一次写请求从提交到落库            | 集群长什么样、对象模型是什么、一次写请求要过几关、一次声明怎么被收敛成事实；关卡只写顺序与失败语义，扩展点的类型与 API 版本治理由第 03 章拥有      | 01       |
| 03  | 扩展机制与 API 治理 | 一个外部类型从注册到被约束          | API 的可扩展面：CRD 与版本演进、聚合 API、控制器运行时、准入 webhook 与 CEL 策略，以及 API 这门学问的治理             | 02       |
| 04  | 工作负载与控制器     | 一份清单到一组被维持的副本          | 清单的组织与交付（模板化、服务端应用与漂移）、Pod 与控制器家族、发布与回滚、有状态与一次性任务各自的收敛语义                         | 02、03    |
| 05  | 调度与放置        | 一个 Pod 从 Pending 到绑定   | 一个 Pod "放在哪"：过滤与打分、亲和与污点、优先级与抢占、拓扑打散，以及调度器在想什么                                   | 02、04    |
| 06  | 资源、容量与争抢     | 一份资源声明到驱逐顺序            | 一个容器"能用多少、谁先被赶、代价是多少"：requests/limits、QoS、驱逐与回收、配额、节点资源管理、自动伸缩机制、成本与装箱效率         | 02、04、05 |
| 07  | 网络           | 一条 Service 声明到一条可用流量路径 | 集群网络模型、Service 与 EndpointSlice、DNS、策略、L7 入口与流量治理（含网格的编排面）、CNI 与多网络               | 02       |
| 08  | 存储与数据        | 一条 PVC 声明到可用的数据面       | 一条 PVC 声明怎么变成可用的数据面：Volume、PV/PVC/StorageClass、CSI 链路、扩容/快照/克隆、状态与数据一致性          | 02       |
| 09  | 安全与多租户       | 一次请求的身份与边界判定           | 谁能对什么做什么、边界画在哪：策略内容的判定、工作负载加固、凭据与密钥生命周期、供应链的强制、审计、隔离边界；扩展机制与 API 治理不在此章          | 02、03    |
| 10  | 生产运维与可观测性    | 一次告警到复盘的时间线            | 怎么看清系统、怎么改变系统：三条链路与告警、容量规划与成本、节点池与节点基线、集群生命周期与控制面（含 etcd）运维、升级与版本兼容、备份恢复与演练、故障处置 | 02–09    |
| 11  | GPU / AI 编排  | 一次 GPU 资源请求到一次作业       | GPU 如何变成可调度资源、复用与隔离、拓扑感知与 gang 调度、作业层与队列；主机与网络数据面不在此章                            | 04、05、06 |
| 12  | 全库收口         | —                      | 跨章接缝、端到端处置链、盲区定位、能力自检（对象是读者脑中的模型，不套分层）                                           | 01–11    |

### 重写进度

已成章的是 01、02（目录形式）；其余章仍是从前留存的单篇旧稿，按上表章号逐个替换：04 ← `02_工作负载与控制器.md`，05 / 06 ← `03_调度_资源_QoS.md`，07 ← `04_网络.md`，08 ← `05_存储.md`，09 ← `06_安全.md`，10 ← `07_生产运维与可观测性.md`，11 ← `08_GPU_AI场景.md`，12 ← `09_复习与自测.md`。第 03 章是新章、无旧稿，素材取自原第 02 章移出的两篇（原文见 git 历史）。被替换的旧稿删除。

### 已裁定的分工

跨章主题的分工只写已经裁定过的部分，其余写作时按当章需要自行把握。通用规则一条：**主归属章写机制与判据，其余章只写自己那一段，同一机制不重讲第二遍。**

- 准入的关卡顺序与失败语义在第 02 章；扩展点类型、准入策略机制与 API 版本治理在第 03 章；策略内容与租户边界在第 09 章。
- 清单交付（模板化、服务端应用与漂移）在第 04 章；交付工具自身的实现不入库。
- 自动伸缩的机制与判据在第 06 章；容量规划与扩容流程在第 10 章；成本与装箱在第 06 章。
- etcd 的一致性与多数派原理在第 02 章；快照、恢复、成员变更与容量在第 10 章。
- 集群生命周期、控制面运维、节点池与节点基线、分层演练在第 10 章；多集群只保留纳管与批量升级窗口。

### 层的判定

沿收敛链依次问下面几句，问到哪句就落在哪层：

| 问句 | 落层 |
| --- | --- |
| 这句话在讲**声明本身**（字段契约、准入把它变成什么、API 语义），或**为什么必须这么设计** | 原理 |
| 在讲**谁 watch 它、按什么顺序收敛、`status` 怎么从一件事变成另一件事** | 体系 |
| 在讲**用哪把工具看哪个字段、怎么读、先看什么** | 实操 |
| 在讲**看起来是这个原因、其实是那个原因** | 判例 |
| 在讲**上限配在哪一层、交什么产物、和别的领域怎么接** | 治理 |

每层的**排除项**（越界即拆篇）：原理不写命令与工具清单、处置动作与现场案例；体系不写工具清单、制度设计与参数怎么配；实操不写机制推演与制度设计；判例不写完整流程；治理不写机制原理。

### 章内构成与导航

- 篇目顺序固定：`原理 → 体系 → 实操 → 判例 → 治理`，篇名形如 `NN_层_关键词.md`；层名只有这五个、顺序不得变，**不规定每层或每章的篇数**。某层确实没有有出处的素材就整层不写，并在该章导读的篇目索引里留一行说明。
- 每章 `00_导读与知识地图.md` 写四块：知识地图（本章主链与横向关系）、术语速查（只收本章新引入的词）、关键字段的读法、篇目索引；**故障现场定位表放在本章实操篇**。导读不写参考文档，参考文档放各篇末尾。**第 01 章的导读作为遗留例外仍带一份参考文档**，由该章重写时按本节规则处理，其余各章不再出现例外。
- 第 12 章不套分层（对象是读者脑中的模型），篇名用 `NN_收口_关键词.md`，内部结构按收口需要自定。
- 每篇具体的节与顺序不在这里规定，写作时按已成章的写法自行把握；每篇结尾必备一句 `**第一反应不要做什么**：…`。

## 三、写作形态

- **行文用学术报告语体**：写完整句，主语与谓语齐全，句间用"因此 / 由于 / 若 / 反之 / 同时"这类显式逻辑连接；不把结论压缩成电报式的名词短语堆叠。
- **不用比喻与黑话代替机制本名**：把机制、占用与关系按本名写出来；任何结论都要落回字段名、单位、数值或错误文本。自检口径：把任意一句单独摘出来，读者不需要再问"这是什么意思"。
- **不使用跨章引用**：正文与导航篇都不写"见第 N 章""第 N 章"这类指路，也不写跨章的篇名。只允许**排除式划界**：用一句"这不是本篇负责的内容"划清范围，**不给方向**。正文同样不写"前面几节""上一节""后面会讲""下一节的前提"这类承接式指路：要承接上文就把结论重述一遍，要划界就用排除式表述。篇内的锚点式编号（`2.3`、`第 6 步`）不受此限，但不得指向本篇之外的篇。
- **黑话只在机制本名同句出现时可保留**：`水位`（文件系统已用容量）、`白障`（whiteout）这类社区用词，在**机制本名已在同一句内出现**时允许保留；只写黑话、不写本名的，不算通过。这类词若收进术语速查，首次出现处必须同时给出英文原词。
- **不使用 wikilink**（`[[...]]`），本章导读的篇目索引除外。引用块只用于呈现逐层演进中被剖析的回答。
- **不讨论文档自身结构**：不写"这篇讲什么""必须先读什么""本文将分三部分""30 秒速览""怎么用这篇笔记"，也不出现 `L1/L2`、`M1–M8` 这类等级与技能编号。
- **给出数值就要给出读法**：凡让读者看某个字段，必须同时写清五件事——是什么、单位、判据、与谁同看、看到它转向哪一步。贴命令输出必须声明"数值仅示意、不代表你的环境"。
- 术语首次出现给中文解释 + 英文原词，缩写展开；**"首次"以章为界**，本章导读的术语速查负责本章新引入的词，前面章已展开过的词在本章不再重复解释。
- 破坏性操作标"只在可丢的实验集群"；只读观测标"生产集群上也可以做"。

## 四、事实纪律

**版本口径**

- **全库对照当前稳定版 1.37 与同主版本的官方文档与上游源码**；默认值、字段口径、版本门都以它们为准。本口径写在这里一次，各章正文不重复声明版本基线。与目标集群同一主版本取文档：老版本文档里的默认值已经变过，`latest` 描述的是尚未进入发行版的东西。
- **版本门必须写成阶段式，并给出处**：写成"弃用并告警 → 引入门控 → 默认关闭 → 锁定为不可再启用 → 删除门控"这样的阶段序列，每一段的版本号与 KEP 编号从下面的版本舞台表取值。只写"某个版本移除"会让人误判动手时机。
- **默认值要写清它是不是默认值、作用在什么范围**，且**不凭记忆写**：策略校验的字段与加固建议的字段常常不是一伙的（`readOnlyRootFilesystem` 是加固建议，不是 Pod Security Standards `restricted` 档的判定项），写混就是事实错误。取不到就写"未确认"，并说明还缺什么才能确认。
- **复核触发**：每当有新的次版本进入稳定发布，在下一个小版本发布之前完成三件事——逐行核对版本舞台表、核对各章默认值的现场取值方式、核对弃用与移除清单。先改本文件的表，再改正文；两处不一致时以本表为准。

**版本舞台表（全库唯一出处）**

版本门与默认值的版本号只在本表维护。表中每一行必须给出 KEP 编号或官方文档锚点；取不到就写"未确认"，并写明还缺什么才能确认。**未确认的条目不得写进正文的具体版本号里。**

| 主题 | 阶段序列 | 出处 | 状态 |
| --- | --- | --- | --- |
| `kube-proxy` 的 `ipvs` 模式 | 弃用并告警 → 引入门控 → 默认关闭 → 锁定为不可再启用 → 删除门控 | `kubernetes/enhancements` 对应 KEP | 未确认：现有材料对阶段与终点有三种互相矛盾的说法，需按 KEP 定稿后再写入各章 |
| 节点代理从运行时探测 cgroup 驱动 | v1.28 引入（alpha，特性门 `KubeletCgroupDriverFromCRI`）→ v1.34 稳定（GA）→ 不支持该查询接口的运行时，回退路径的移除已推迟到 v1.38 | KEP-4033（`kubernetes/enhancements` 的 `keps/sig-node/4033-group-driver-detection-over-cri`）与节点代理配置文档；推迟决定见 `kubernetes/enhancements` PR 6087 与 `kubernetes/kubernetes` PR 139121 | 已确认 |
| `DRA` | v1.30 引入 → v1.35 稳定 | 对应 KEP | 待补出处锚点 |
| `KMS v2` 静态加密 | v1.29 稳定 | 对应 KEP | 待补出处锚点 |

**实验环境与实测纪律**

- **能查文档就不实测；实测必须标环境与版本。** 实测分两档：**单机与 kind 档（默认）**用 Docker + containerd 与 kind 多节点集群跑 1.37，网络与 CSI 插件按需；**引导集群档**用 3 节点 `kubeadm` 加独立 etcd 或云上三台主机，控制面高可用、证书轮换与到期、etcd 快照与恢复、逐版本升级、驱逐与 `drain` 的长期行为必须在这一档做。
- **不允许用 kind 档的读数支撑**：etcd 恢复的 RTO 与 RPO、升级路径与版本偏斜、控制面容量与 etcd 性能、驱逐与回收的阈值行为。
- **上机结果不等于普遍结论**：命令输出与集群版本、发行版、CNI/CSI、节点规格强相关。没出现预想结果优先怀疑环境不匹配，而不是先改结论；实机结果写进正文必须写清环境，并说明可能只在该环境成立。字段名、路径、命令输出格式、版本门控这类客观事实可以也应当上机核对。
- 集群形态、插件与版本随篇注明；临时脚本、原始输出、草稿报告**不入库**，正文不引用它们。

**边界：哪些内容不属于本库**

| 内容 | 归哪 |
| --- | --- |
| GPU 驱动与内核模块、PCIe/NUMA 拓扑、RoCE/IB 无损网络、perftest、NCCL 排障、DCGM 指标与 XID 判读 | AI 基础设施主题域（主机与网络数据面） |
| 主机侧通用能力与调优：systemd、内存与 OOM、文件系统、TCP 排障、性能方法论、IRQ 亲和、软中断、MTU、网卡环形缓冲 | Linux 主题域 |
| 容器运行时与镜像在节点上的观测细节、制品仓库与镜像构建服务的搭建运维 | 第 01 章只写制品侧语义 |
| 容器侧数据面：节点上的虚拟网卡与桥接、端口映射、绑定挂载的传播方式 | 编排层的数据面两章 |
| Device Plugin、DRA、GPU Operator 的编排面、拓扑感知与 gang 调度、作业层与队列 | 第 11 章 |
| 清单交付工具自身的实现：模板语言、GitOps 控制器的部署与调优 | 工具官方文档；第 04 章只写与收敛链相关的判据 |
| 集群联邦、跨集群服务发现与流量 | 不属本库；第 10 章只写集群纳管与批量升级窗口 |
| 云厂商计费与商务：账单、预留、折扣 | 不属本库；第 06、11 章只写计量口径与装箱效率 |
| 操作系统镜像的构建与节点镜像流水线、托管集群的产品控制台操作、客户端插件的开发 | 不属本库；第 10 章只写节点池基线与形态约束，第 02 章实操只写客户端用法与字段归属冲突的处置 |

本库只保留**编排层事实**，且对上述边界只做一句排除式划界，不给方向。

## 五、高发错误清单

本节是第二至第四节规则的反例索引；规则修订时同步核对本节。以下是本次全库审计（对旧 `01`–`09` 九篇）确认的错误类型，重写时逐条对照：

1. **只写终点版本**：把"某个版本移除"写成结论，漏掉中间几段阶段门。
2. **策略字段与建议字段混为一谈**：把加固建议写成 PSS 档位的判定项。
3. **口径打架**：同一件事两处写且数值或字段名不一致，或同一篇的表格与正文给出两套口径（含计数与自述不符）。
4. **归属错位的低配重复**：把更深的专业篇章的内容在本章重讲一遍且讲得更浅（分工见第二节的已裁定的分工）。
5. **机制归因错位**：把观察值安到错误的开关上。
6. **文档自身结构声明**：目标、关联复习、30 秒速览、怎么用这篇笔记、回到路线图。
7. **给出数值不给读法**：只写"看某个字段"，没写是什么、单位、判据、与谁同看、转向哪一步。
8. **输出样例未声明仅示意**，或把自己的实测值当通用结论。
9. **破坏性操作缺边界**：没有实验集群限制、没有回滚步骤、没有上限控制。
10. **术语类别混淆**：准入控制器与准入插件、命令与内核接口文件、CRD 与内置对象、扩展资源与原生资源。
11. **用比喻或黑话代替机制本名**，让读者先解码措辞才能理解结论。
12. **低级遗漏**：缩写未展开、跨章 wikilink、篇末缺边界句。

## 六、定稿自检

每篇写完立即逐条走：

1. 只有一个一级标题；除本章导读外没有 wikilink；引用块只出现在逐层演进处；
2. 篇末有「第一反应不要做什么」；
3. 正文无章号、无跨章指路、无"这篇讲什么"式声明、无应试口径；
4. 术语首次出现有中文解释 + 英文原词，缩写已展开；
5. 每个被引用的字段都给了"是什么 / 单位 / 判据 / 与谁同看 / 转向哪一步"；输出样例声明了仅示意或写明了环境；
6. 每个版本门都是阶段式且与版本舞台表一致；默认值带版本与现场取值方式；
7. 每层的内容没有越界；破坏性操作有边界与回滚；同一结论在本章没有两份写法，且与本章导读的篇目索引一致。

## 七、必须同步

改章或篇的构成（增删、改名、调序）后，同步三处：

1. 本文件的第二节结构表、已裁定的分工与重写进度；
2. 该章 `00_导读与知识地图.md` 的知识地图、术语速查、关键字段的读法、篇目索引；
3. 本文件的版本舞台表（只要涉及版本门、默认值或弃用清单）。

根 `README.md` 只保留库列表、主题、状态与入口，**不写篇数、章表与进度**：它是唯一允许出现本库章级入口链接的位置，但因为不含统计数字，增删章篇都不需要改它，只有增删库或改入口时才同步。篇数一律现场数文件，不把统计数字写进任何一份文档。

跨库引用（与 Linux、AI 基础设施两份规约的边界表）按排除式表述维护：只说明某块内容归哪个库，不写章号、不写篇名、不给链接。

## 八、参考

- Kubernetes 官方文档与上游仓库（`kubernetes/kubernetes`、`kubernetes/website`、`kubernetes/enhancements` 的 KEP）——字段、默认值、版本门的第一出处；版本舞台表的每一行都从这里取值。
- OCI Image / Distribution / Runtime 三份规范——第 01 章的制品与运行时语义。
- 具体运行时（containerd、CRI-O）与网络 / 存储插件的官方文档——节点侧与数据面配置的现场口径。
- CNCF 项目文档（Gateway API、VolumeSnapshot、cert-manager、Kueue、Volcano、NVIDIA GPU Operator）——第 03 章所讲的标准 CRD 与第 11 章的编排面。
- 中文命令速查站 `https://cmd.ifdev.cn/c/{cmd}.html` 只用来快速摸清一个命令有哪些子命令；写进正文的选项名、默认值、单位与字段口径一律回官方文档核过。
